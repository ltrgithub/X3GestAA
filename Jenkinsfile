#!groovy

pipeline {
    agent any
    stages {
        stage('Automatic test QLF') {
            steps {
                echo 'Qlf test'
            }
        }
	stage('Automatic test WebDriver') {
            steps {
                echo 'Webdriver'
            }
        }
    }
}
