<Page
    x:Class="SageX3WUP.App.Pages.WebViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SageX3WUP.App.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#FF006C30">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="150" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <WebView x:Name="webView" Grid.RowSpan="3"/>
        <StackPanel x:Name="panelLoading" Margin="0" VerticalAlignment="Center" Background="White" Grid.Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="Loading" Height="72" FontSize="48" TextAlignment="Center"/>
                <TextBlock x:Name="textBlockName" TextWrapping="Wrap" Text="config" FontSize="48" Margin="20,0,0,0"/>
            </StackPanel>
            <ProgressBar Height="50" VerticalAlignment="Stretch" IsIndeterminate="True" Margin="0"/>
        </StackPanel>
    </Grid>
</Page>
