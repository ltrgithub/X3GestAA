'use strict';

exports.contract = {
	application: 'syracuse',
	contract: 'support',
	resources: function() {}, //require('./strings').resources,
	entities: {
		traceImport: require('./entities/traceImport').entity,
		traceImportSelect: require('./entities/traceImportSelect').entity,
		trace: require('./entities/trace').entity
	},
	representations: {
		//trace: require('syracuse-trace/lib/representations/trace').representation
	},
	searchFacets: {},
	dbMeta: {
		initScript: [],
		updateScript: [{
			script: __dirname + '/updateScripts/updateScript',
			version: 1
		}]
	}
};
