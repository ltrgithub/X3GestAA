"use strict";

// var flashWrapper = require("syracuse-ui/lib/field/chart/flashWrapper").flashWrapper;
// var flashWidget = require("syracuse-ui/lib/field/chart/flashWidget").FlashWidget;
var helpers = require("syracuse-core/lib/helpers");
var aqff = require("../fixtures/aqff-en-US");

QUnit.module(module.id);

test("agenda initial data", function() {

	// var article = {
	// 	id: "1-1",
	// 	$prototype: aqff.$prototype
	// };

	// var agenda = new flashWidget();
	// agenda.id = "1-1";
	// agenda.$prototype= aqff.$prototype;
	// agenda.$item = aqff.$prototype.$article.$layout.$items[1].$items[3].$layout.$items[0].$items.$layout.$items[0];

	// var wrapper = agenda._createWrapper();
	var wrapper = {};

	ok(wrapper, "wrapper not null");
});