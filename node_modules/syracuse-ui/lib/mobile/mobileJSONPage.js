"use strict";
var helpers = require("syracuse-core/lib/helpers");
var DesktopPage = require("syracuse-ui/lib/page/desktopPage").DesktopPage;
var locale = require('syracuse-core/lib/locale');

function MobileJSONPage() {}

exports.MobileJSONPage = helpers.defineClass(MobileJSONPage, DesktopPage, {
	dispose: function() {
		this.mobileLocalize = null;
		DesktopPage.prototype.dispose.call(this);
	},
	loadBox: function($fieldsPrototype, initData) {
		this.$pageCategory = "page";
		this.mobileLocalize = locale.resources(module)();
		this.$prototype = {
			$properties: {
				$fieldPrototype: {
					"$type": "application/x-string"
				},
				$fieldJSON: {
					"$type": "application/x-string"
				}
			},
			$actions: {
				$applyChange: {
					$title: this.mobileLocalize.p_applychange
				}
			}
		};
		this.$item = {
			$layout: {
				$items: [{
					$skin: "s-list-menus-record-menus-link",
					$category: "link",
					$bind: "$applyChange"

				}, {
					$layoutType: "tabs",
					$items: [{
						$category: "section",
						$title: this.mobileLocalize.p_fieldJSON,
						$layout: {
							$items: [{
								$bind: "$fieldJSON",
								$isTitleHidden: true,
								$isEditMode: true,
								$rows: 50,
								$isAutoSizeDisabled: true,
								$skin: "s-json-field"
							}]
						}

					}, {
						$category: "section",
						$title: this.mobileLocalize.p_fieldPrototype,
						$layout: {
							$items: [{
								$bind: "$fieldPrototype",
								$isTitleHidden: true,
								$isEditMode: true,
								$rows: 50,
								$isAutoSizeDisabled: true,
								$skin: "s-json-field"
							}]
						}
					}]
				}]
			}
		};
		this.$autoFetch = false;
		DesktopPage.prototype.loadBox.call(this, initData);
		this.header.style.display = "none";
		this.body.style.padding = "0.5em";
	},
	notifyActionChange: function(menuItem, target, value) {
		if (value.$applyChange) {
			var diag = null;
			try {
				var field = this.boundFields.$fieldJSON[0];
				var $article = JSON.parse(field.getDataValue());
				field.setInputValue(JSON.stringify($article, null, 2));
				document.site.mainPage.$item = $article;
				document.site.mobileGateway.isApplyChangeDisabled = true;
				document.site.mainPage.designer.historyTool.notifyUpdate(true);
				delete document.site.mobileGateway.isApplyChangeDisabled;
				document.site.mainPage.designer.historyTool.onAfterStepChange(helpers.object.clone($article, true));
				diag = {
					$message: this.mobileLocalize.p_changeapplied,
					$severity: "success"
				};
			} catch (error) {
				diag = {
					$message: error.message
				};
			} finally {
				delete document.site.mobileGateway.isApplyChangeDisabled;
				this.showDiagnoses({
					autoHide: diag.$severity == "success" ? {
						autoHideTimeOut: 3000
					} : null,
					$diagnoses: [diag]
				});
			}
			return;
		}
		DesktopPage.prototype.notifyActionChange.call(this, menuItem, target, value);
	},
	fillEditors: function($prototype, $article) {
		this.applyChange({
			$fieldJSON: JSON.stringify($article, null, 2),
			$fieldPrototype: JSON.stringify($prototype, null, 2)

		});
	}
});