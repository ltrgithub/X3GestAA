"use strict";

var _locker, _overlay, _dummy, _timeout;


exports.lock = function(delay, isLong, forceFocusLoss) {
	if (!_locker) {
		_locker = document.createElement('div');
		_locker.className = "s-uiLocker-loading";
		_dummy = document.createElement('a');
		_dummy.setAttribute("href", "#");
		_dummy.syrUILockerBlackHole = true;
		_locker.appendChild(_dummy);
		syra_site.layoutSlot.appendChild(_locker);
	}
	_locker.style.display = "";
	if (forceFocusLoss) {
		_dummy.focus();
	}
	_timeout = setTimeout(function() {
		if (isLong) {
			if (!_overlay) {
				_overlay = document.createElement('div');
				_overlay.className = "s-uiLocker-overlay";
				syra_site.layoutSlot.appendChild(_overlay);
			}
			_overlay.style.display = "";
		}
		exports.ensureLock();
	}, (delay || 0) > 0 ? delay : 0);
};

exports.ensureLock = function() {
	if (_locker && _locker.style.display !== "none") {
		var site = syra_site;
		site.dom.setZIndex(_locker);
		var width = site.layoutSlot.clientWidth,
			height = site.layoutSlot.clientHeight;
		var top = 0;
		if (site.header) {
			var headerH = site.header.clientHeight;
			if (headerH) {
				top = headerH;
				height -= top;
			}
		}
		var style = _locker.style;
		style.top = top + "px";
		style.width = width + "px";
		style.height = height + "px";

		style = _overlay && _overlay.style;
		if (style && style.display !== "none") {
			style.top = top + "px";
			style.width = width + "px";
			style.height = height + "px";
			site.dom.setZIndex(_overlay);
		}
	}
};

exports.unlock = function() {
	var style;
	if (_locker) {
		style = _locker.style;
		style.display = "none";
		style.width = "0px";
		style.width = "0px";
	}
	if (_overlay) {
		style = _overlay.style;
		style.display = "none";
		style.width = "0px";
		style.width = "0px";
	}
	if (_timeout) {
		clearTimeout(_timeout);
		_timeout = null;
	}
};
exports.dispose = function() {
	_locker = _overlay = _dummy = _timeout = null;
};