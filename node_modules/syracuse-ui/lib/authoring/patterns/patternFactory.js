"use strict";
var helpers = require('syracuse-core/lib/helpers');
var categories = {
    "headerTabs": require("./headerTabsPattern").HeaderTabsPattern
};

exports.build = function($category, $prototype, $sourceArticle){
    var pattern = new categories[$category]();
    return pattern.build($prototype, helpers.object.clone($sourceArticle, true));
};
