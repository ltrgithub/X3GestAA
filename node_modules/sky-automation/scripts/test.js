var console = require('../lib/helpers').console;

var counter = 100;

function step() {
	console.log("I-phase: counter=" + counter);
	if (--counter > 0) setTimeout(step, 100);
}

exports.step = step;
