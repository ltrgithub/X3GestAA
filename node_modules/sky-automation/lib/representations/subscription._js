'use strict';

module.exports = {
	$entityName: 'subscription',
	$facets: {
		$details: {
			$layout: {
				$items: [{
					'$category': 'section',
					$layout: {
						$items: [{
							$bind: 'site'
						}, {
							$bind: 'valid'
						}, {
							$bind: 'license',
							$rows: 10
						}]
					}
				}]
			}
		},
		$edit: {
			$copy: '$details'
		},
		$query: {
			$layout: {
				$items: [{
					$category: 'section',
					$layout: {
						$items: [{
							$category: 'section',
							$layout: {
								$items: [{
									$bind: '$resources',
									$layout: {
										$items: [{
											$bind: 'site'
										}, {
											$bind: 'valid'
										}]
									}
								}]
							}
						}]
					}
				}]
			}
		}
	}
};
