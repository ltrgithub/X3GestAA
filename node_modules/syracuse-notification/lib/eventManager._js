"use strict";

var helpers = require('syracuse-core/lib/helpers');
var tracer = require("syracuse-trace/lib/helper").getTracer("notifications");
var globals = require('streamline/lib/globals');
var uuid = require('syracuse-core/lib/helpers').uuid;
var util = require('util');
var adminHelper = require("syracuse-collaboration/lib/helpers").AdminHelper;
var mongodb = require('streamline-mongodb');

var db;

var Event = helpers.defineClass(function(_, type, data, from, to, severity) {

	this.type = type;
	this.from = from;
	this.to = from;
	this.severity = severity;
	this.data = data;

}, null, {
	serialize: function() {
		return {
			_id: uuid.generate(),
			type: this.type,
			stamp: new Date().now,
			to: this.to,
			from: this.from,
			severity: this.severity,
			data: this.data
		};
	}
});


var EventMessage = helpers.defineClass(function(_, title, message, from, to, severity) {
	Event.call(this, _, "message", {
			thumbnail: null,
			title: title,
			message: message
		},
		from, to, severity);
}, Event, {});

function getEventCollection(_) {
	db = db || adminHelper.getCollaborationOrm(_);
	return db.db.collection("NotificationEvent", _);
}


exports.createMessage = function(_, title, message, from, to, severity) {
	tracer.info && tracer.info("Create message");
	var evt = new EventMessage(_, title, message, from, to, severity).serialize();
	getEventCollection(_).insert(evt, _);
	return evt;
};

exports.getEvents = function(_, count) {
	tracer.info && tracer.info("Get events");
	var events = getEventCollection(_).find({}).toArray(_);
	//console.log("Events1: "+JSON.stringify(events,null,2));
	return events;
};