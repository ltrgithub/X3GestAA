"use strict";

var UT = require("./../lib/ut");



describe("Login", function() {
	// Open browser, login, ensure browser is closed once all tests were executed
	//
	// Login/quit explicitly handled in the test case below
	// UT.defaultBeforeAndAfter();
	//

	before(function() {
		this.timeout(15000);
		return UT.login();
	});

	it("Login - Do Login", function() {
		// do nothing, this is just to do the "before" part
	});

	after(function() {
		return UT.Driver.quit();
	});
});