"use strict";

var UT = require("./../lib/ut");
//--- See ../autoUI/config.js ----------------------------------------
// "emailSageId"    : "SageX3Mobility@gmail.com",
// "passwordSageId" : "Qualit122016%",
// ---------------------------------------------------------
describe("Login/Logout with SageId", function() {
	before(function() {
		this.timeout(20000);
		return UT.loginSageId({
			testPageUrl: "html://HOME",
			pagewait: "HOME"
		});
	});

	it("Login - Do Login", function() {
		// do nothing, this is just to do the "before" part
	});

	//-- Ltr 24/11/2016 ----- if not error on the next script --
	it("Logout for Login", function() {
		this.timeout(20000);
		return UT.App.logout();
	});

	after(function() {
		if (UT.Config.dontQuit) {
			return;
		}
		//return UT.Driver.quit();
	});
});