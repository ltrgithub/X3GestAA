<div class="s-home-container">
	<h1>CUSTOMER HOME</h1>
	<div class="list-group">
		{{#each itemList}} 
			 <a href="#" data-nav="customers.detail" data-detailid="{{uuid}}" class="list-group-item active">{{@index}} - {{name}}</a>
		{{/each}}
	</div>
</div>