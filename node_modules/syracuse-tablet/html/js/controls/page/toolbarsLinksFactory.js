"use strict";

var locale = require('syracuse-tablet/html/js/helpers/locale');
var fontUtils = require('syracuse-tablet/html/js/ui/fontUtils');

var linksList = {
	/************************
	 ** HEADER
	 *************************/
	designPage: {
		label: locale.text("header.links.designPage"),
		fontClass: fontUtils.pageIcon("designPage"),
		dataParams: "designPage"
	},
	clearCache: {
		label: locale.text("header.links.clearCache"),
		fontClass: fontUtils.pageIcon("clearCache"),
		dataParams: "clearCache"
	},
	switchContext: {
		label: locale.text("header.links.switchContext"),
		fontClass: fontUtils.pageIcon("switchContext"),
		dataParams: "switchContext"
	}
};