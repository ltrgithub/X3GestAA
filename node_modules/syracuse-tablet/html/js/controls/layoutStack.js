"use strict";

var utils = require('syracuse-tablet/html/js/helpers/utils');
var Base = require('syracuse-tablet/html/js/controls/layoutBase').Klass;
var BuilderLytStackHtml = require('syracuse-tablet/html/js/controls/builderLytHtml').BuilderLytStackHtml;

/**
 * cssOptions
 * 		cssLayout, cssCell: class added to default ones
 * default classes can  be overridden
 */
var _Klass = utils.defineClass(
	function(controller, type, article) {
		var self = this;
		Base.call(self, controller, type, article);
	}, Base, {
		defaultDisplayOptions: function() {
			return {
				cssDefLayout: ["container s-m-layout s-m-stack"],
				cssDefCell: ["s-m-layout-cell"]
			};
		},
		buildHtml: function(html, controllerDao, buildOptions) {
			var self = this;
			if (!self.builder) {
				self.builder = new BuilderLytStackHtml(self);
			}
			self.builder.buildHtml(html, controllerDao, buildOptions);
		}
	});

exports.Klass = _Klass;