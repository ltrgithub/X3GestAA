"use strict";

var Base = require('syracuse-tablet/html/js/controls/panels/aside/panelAside').PanelAside;
var locale = require('syracuse-tablet/html/js/helpers/locale');
var utils = require('syracuse-tablet/html/js/helpers/utils');
var panelsHtml = require('syracuse-tablet/html/js/controls/panels/panelsHtml');

var _availableSlots = ["actions", "links", "main", "array", "tools", "home", "unclassified"]
	/**
	 * 
	 */
exports.PanelActions = utils.defineClass(
	function PanelActions(controller, type, article, opts) {
		Base.call(this, controller, type, article, opts);
	}, Base, {
		buildHtml: function() {
			Base.prototype.buildHtml.call(this);
		},
		_getItems: function() {
			return this.actionAdapter.getActionsPanelItems();
		},
		_extendContext: function(ctx) {
			ctx.title = locale.text("actions");
			return ctx
		},
		_getAvailableSlots: function() {
			return _availableSlots
		},
		_getAdditionnalCss: function() {
			return "";
		},
		_getItemHtml: function(item) {
			return panelsHtml.getItemHtml("actionPanel", item)
		},
		show: function() {
			if (this.$$elmt) {
				this.buildHtmlContaint();

			}
			Base.prototype.show.call(this);
		},
	});