"use strict";

/*
 * WebSQL storage interface (Chrome, Safari)
 */

/**
 * The top class is storageWebSQLDrafts
 * It's just a trick to split the code that manages Draft from the one that manages DStata in two sub classes of storageWebSQLBase
 */
var _storageInterface = require('syracuse-tablet/html/js/storage/storageWebSQLDrafts').Klass;


exports.getStorageInterface = function() {
	return _storageInterface;
};
exports.isAvailable = function() {
	return (window.openDatabase != null);
};