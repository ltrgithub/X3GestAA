"use strict";

var log = require('syracuse-tablet/html/js/helpers/logger').log;
var utils = require('syracuse-tablet/html/js/helpers/utils');
var alphanum = require('syracuse-tablet/html/js/controls/alphanum').Control;
var numeric = require('syracuse-tablet/html/js/controls/numeric').Control;
var checkBox = require('syracuse-tablet/html/js/controls/checkBox').Control;
var combo = require('syracuse-tablet/html/js/controls/combo').Control;
var date = require('syracuse-tablet/html/js/controls/date').Control;
var grid = require('syracuse-tablet/html/js/controls/grid').Control;
var reference = require('syracuse-tablet/html/js/controls/reference').Control;


var _createControl = function(controller, type, options) {
	switch (type) {
		case "application/x-string":
		case "application/x-password":
		case "text/plain":
			return new alphanum(controller, type, options);
			break;
		case "application/x-integer":
		case "application/x-real":
		case "application/x-decimal":
		case "application/x-quantity":
			return new numeric(controller, type, options);
			break;
		case "application/x-choice":
			return new combo(controller, type, options);
			break;
		case "application/x-boolean"
		return new checkBox(controller, type, options);
		break;
		case "application/x-date":
		case "application/x-time":
		case "application/x-datetime":
			return new date(controller, type, options);
			break;
		case "application/x-reference":
			return new reference(controller, type, options);
			break;
		case "application/x-array":
			return new grid(controller, type, options);
			break;
		default:
			throw new Exception("Unknown control type[" + type + "]");
			break;
	}
}

exports.createControl = _createControl;