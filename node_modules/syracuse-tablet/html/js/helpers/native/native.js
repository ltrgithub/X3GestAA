"use strict";

var nativeShare = require('syracuse-tablet/html/js/helpers/native/nativeShare');
var nativeLiveTile = require('syracuse-tablet/html/js/helpers/native/nativeLiveTile');
var nativeScheduleMsg = require('syracuse-tablet/html/js/helpers/native/nativeScheduleMsg');
var nativeMessages = require('syracuse-tablet/html/js/helpers/native/nativeMessages');
var nativePenInput = require('syracuse-tablet/html/js/helpers/native/nativePenInput');
var nativeVoiceCommands = require('syracuse-tablet/html/js/helpers/native/nativeVoiceCommands');
var nativeSageX3WUPApp = require('syracuse-tablet/html/js/helpers/native/nativeSageX3WUPApp');

exports.hasCapability = function(capability) {
	return getNativeModule(capability) != null;
};
exports.getModule: function(capability) {
	switch (capability) {
		case "liveTile":
			return nativeLiveTile && nativeLiveTile.supportsLiveTile() ? nativeLiveTile : null;
		case "scheduleMessage":
			return nativeScheduleMsg && nativeScheduleMsg.supportsScheduleMsg() ? nativeScheduleMsg : null;
		case "share":
			return nativeShare && nativeShare.supportsShare() ? nativeShare : null;
		case "toastMessages":
			return nativeMessages && nativeMessages.supportsShowToastMessage() ? nativeMessages : null;
		case "penInput":
			return nativePenInput && nativePenInput.supportsPenInput() ? nativePenInput : null;
		case "voiceCommands":
			return nativeVoiceCommands && nativeVoiceCommands.supportsVoiceCommands() ? nativeVoiceCommands : null;
		case "X3WUPApp":
			return nativeSageX3WUPApp && nativeSageX3WUPApp.isSageX3WUPApp() ? nativeSageX3WUPApp : null;
		default:
			console.log("Unknown native module " + capability)
			return null;
	}
}