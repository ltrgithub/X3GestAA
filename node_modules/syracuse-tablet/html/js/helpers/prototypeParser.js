"use strict";

var log = require('syracuse-tablet/html/js/helpers/logger').getLogger("protoParser");
var utils = require('syracuse-tablet/html/js/helpers/utils');

/**
 * Parses a JSON prototype and returns a default JSON $article
 *  	prototype: JSON
 */
var _proto2Article = function(prototype) {
	var props = prototype.$properties;
	var article = {
		$layoutType: "stack",
		$items: []
	};
	for (var prop in props) {
		article.$items.push({
			$bind: prop
		});
	}
	return article;
};

/**
 * Parses an x-array JSON prototype and returns a default JSON $article
 *  	prototype: JSON
 */
/*
var _array2Article = function(prototype) {
	var item = prototype.data["$item"];
	if (!item) throw new Error("prototype.$item expected");
	var article = {
			$layoutType: "stack",
			$items: [{
				$layoutType: "row",
			},{
				
			}]
		};
	var idx = 0;
	var row = {
		$layoutType: "row",
		$items:[]
	};
	var props = item.$properties;
	for (var prop in props) {
		row.$items.push({
			$bind: prop
		});
	}
	


	return article;
};
*/
exports.proto2Article = _proto2Article;
//exports.array2Article = _array2Article;