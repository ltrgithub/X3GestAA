"use strict";

var _formatApi = {
	"application/x-integer": require('syracuse-tablet/html/js/helpers/types/integer'),
	"application/x-decimal": require('syracuse-tablet/html/js/helpers/types/decimal'),
	"application/x-real": require('syracuse-tablet/html/js/helpers/types/real'),
	"application/x-date": require('syracuse-tablet/html/js/helpers/types/date'),
	"application/x-time": require('syracuse-tablet/html/js/helpers/types/time'),
	"application/x-datetime": require('syracuse-tablet/html/js/helpers/types/datetime')
};

var utils = require('syracuse-tablet/html/js/helpers/utils');

exports.Formatter = utils.defineClass(function() {}, null, {

	/*
	 * Format a value for displaying it
	 *
	 * In case of dates
	 * This will eventually resolve LONG month names to e.g. "April" instead of 04
	 */
	formatDisplay: function(value, fmt) {

	},

	/*
	 * Format a value for displaying+editing it
	 *
	 * In case of dates:
	 * This will never resolve LONG month names to e.g. "April" but always
	 * resolve to numerical reperesentation like 04 for easy user input
	 */
	formatEdit: function(value, fmt) {

	},

	/* 
	 * Parse any kind of input string
	 */
	parseValue: function(value, fmt) {

	}
});