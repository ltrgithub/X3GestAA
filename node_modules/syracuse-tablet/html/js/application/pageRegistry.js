"use strict";

var pageHelper = require('syracuse-tablet/html/js/application/pageHelper');

exports.getPageInfo = function(pageName, options, currentPage) {
	console.log(pageName);
	var pageInfo = pageHelper.getPageInfo(pageName, options, currentPage);
	return $.smResolve(pageInfo);
};