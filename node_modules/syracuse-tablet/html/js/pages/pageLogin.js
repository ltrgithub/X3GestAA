"use strict";

var utils = require('syracuse-tablet/html/js/helpers/utils');
var log = require('syracuse-tablet/html/js/helpers/logger').log;
var Base = require('syracuse-tablet/html/js/pages/page').Page;
var templates = require('syracuse-tablet/html/js/pages/pageTemplates');
var auth = require('syracuse-tablet/html/js/application/authentication');

var _tmplCtx = {
	header: {
		"pageId": "login",
		"headerttl": "Sage X3 Mobile Login Page",
		"forgotpwd": "Forgot password"
	},
	content: {
		"label_user": "User name",
		"placeholder_user": "Enter some text",
		"label_password": "Password",
		"placeholder_password": "Enter some text",
		"act_login": "Login",
		"title": "Login page"
	}
};
var _Page = utils.defineClass(
	function($parent, state) {
		var self = this;
		Base.call(self, $parent, state);
	}, Base, {

		buildHtml: function(state, data) {
			var self = this;
			var deferred = $.Deferred();
			try {
				templates.getTemplate(self.state, _tmplCtx.header, _tmplCtx.content).always(function(result) {
					deferred.resolve(result);
				});
			} catch (e) {
				templates.resolveError(deferred, self.state.name, e);
			} finally {
				return deferred.promise();
			}
		},

		_actLogin: function() {
			var self = this;
			var deferred = $.Deferred();
			var success = self.$elmt.find("#login-success").hide();
			var fail = self.$elmt.find("#login-fail").hide();
			var usr = self.$elmt.find("#login-user").val();
			var pwd = self.$elmt.find("#login-password").val();
			auth.login(usr, pwd).always(function(ok, userProfile, message) {
				if (ok) {
					success.show().html("Login succeeded");
					window.setTimeout(function() {
						// Page login is called with an optional context that indicates if there's a redirection and the reason
						// {redirect:"page", reason:"message"}
						var ctx = self.getProp("context");
						window.smApp.changePage(ctx && ctx.redirect || "");
					}, 2000);
				} else {
					fail.show().html("Login failed<p>" + message + "</p>");
				}

			});
			return deferred.promise();
		}
	});

exports.Page = _Page;