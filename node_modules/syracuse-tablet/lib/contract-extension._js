"use strict";

module.exports = {
	application: "syracuse",
	extends: "collaboration",
	resources: require('./strings').module,
	entities: {
		// Mobile
		mobileApplication: require('./entities/mobileApplication').entity,
		mobileDashboard: require('./entities/mobileDashboard').entity,
		mobileDashboardVignette: require('./entities/mobileDashboardVignette').entity,
		mobileGadget: require('./entities/mobileGadget').entity,
		mobileRepresentation: require('./entities/mobileRepresentation').entity,
		mobileMigrateDashboard: require('./entities/mobileMigrateDashboard').entity,
		mobileGadgetParam: require('./entities/mobileGadgetParam').entity,
		mobileSharedContext: require('./entities/mobileSharedContext').entity,
	},
	representations: {
		// Mobile
		mobileGadget: require('./representations/mobileGadget').representation,
		mobileDashboard: require('./representations/mobileDashboard').representation,
	},
	dbMeta: {
		initScript: [],
		updateScript: [{
			script: __dirname + '/updateScripts/updateScript',
			version: 1
		}]
	}
};