"use strict"

var helpers = require("syracuse-core/lib/helpers");
var DataModel = require("syracuse-orm/lib/dataModel").DataModel;

function X3DataModel(endpoint) {
	this._endpoint = endpoint;
}

exports.DataModel = helpers.defineClass(X3DataModel, DataModel, {

});
