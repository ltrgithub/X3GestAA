import * as util from '../engine/runtime/util';

export interface NodeConfig {
	etna: any;
	etl?: any;
	streamline?: any;
}

export var config: NodeConfig = { etna: {} };
try {
	config = require("../../../../nodelocal").config || { etna: { } };
} catch (ex) {
	console.log(ex);
}

config.etna = config.etna || {};
config.etl = config.etl || {};
config.etl.metaSubFolder = config.etl.metaSubFolder || "etna";

export const trace = config.etna.traceError ? console.log : null;

// no circular references up to now. This object can be exported and used by other code
