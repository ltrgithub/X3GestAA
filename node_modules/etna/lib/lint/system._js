"use strict";

const glob = require('streamline/lib/globals');
import * as util from './util';

export const functions = {
	DBGCALLSTACK: {
		count: 2,
		fn(depth, result) {}
	}
};

exports.functions = ["NOMAP", "MASKCOU", "ADXPID", "ADXTCP", "FREEMEM", "NBRUSER", "ADXPAM", "ADXUSR", "COP$", "TRTCOU", "NOMAP", "ADXNFS", "MASKCLA", "PROGCAN", "PROGLDD", "PROGSIZ", "PROGUSD"].reduce((r, f) => {
	r[f] = {
		count: 0,
		fn() {}
	};
	return r;
}, exports.functions);

exports.functions = ["MASKABR", "FILELEV", "ADXSEEK", "INPMODE", "VARINIT", "MASKLEV", "MASKNBF", "MASKRK", "MASKSIZ", "ADXUID", "MASKNAM", "ADXMAC", "ADXPNO", "GETENV$", "VER$", "ADXCIO", "ADXIOA"].reduce((r, f) => {
	r[f] = {
		count: 1,
		fn(a) {}
	};
	return r;
}, exports.functions);

export const instructions = {
	FILESYS(abbrev, vars, from) {
		const avars = util.instructions.A(vars);

		return function dot$do(_: _) {};
	}
};