"use strict";

const glob = require('streamline/lib/globals');
import * as util from './util';

exports.functions = ["TIME$", "DATE$", "DATETIME$", "TIME"].reduce((r, f) => {
	r[f] = {
		count: 0,
		fn() {}
	};
	return r;
}, {});

exports.functions = ["DAY", "DAYN", "MONTH", "NDAY", "WEEK", "YEAR", "GDATETIME$", "EOMONTH", "NDAY$"].reduce((r, f) => {
	r[f] = {
		count: 1,
		fn(a) {}
	};
	return r;
}, exports.functions);

exports.functions = ["DAY$", "MONTH$"].reduce((r, f) => {
	r[f] = {
		count: 1,
		fn(_, a) {}
	};
	return r;
}, exports.functions);

exports.functions = ["ADDMONTH", "AWEEK"].reduce((r, f) => {
	r[f] = {
		count: 1,
		fn(a, b) {}
	};
	return r;
}, exports.functions);

//count: 3, -- sometimes used with 1 - see AFNC line 588
exports.functions.GDAT$ = {
	fn(d, m, y) {}
};

exports.instructions = {
	DATE(year, month, day) {}
};