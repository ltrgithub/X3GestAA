import { _ } from 'streamline-runtime';
import * as util from '../util';
const Helper = util.Helper;
import { TypePropertyMeta } from '../metadata/type';
import { Supervisor } from '../supervisor';

export class TypePro extends Helper {
	supervisor: Supervisor;
	data: TypePropertyMeta;
	constructor(superv: Supervisor, data: TypePropertyMeta) {
		super();
		this.supervisor = superv;
		this.data = data;
	}

	init(_: _) {
		return this;
	}
};

export const collectionName = 'ATYPEPRO';
export const keyName = 'COD';
export const Constructor = TypePro;