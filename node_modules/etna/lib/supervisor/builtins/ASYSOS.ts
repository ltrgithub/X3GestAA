import { _ } from 'streamline-runtime';
import * as os from 'os';

export const $exported = true;

export function	ARCH(_: _) {
	return os.arch();
}

export function PLATFORM(_: _) {
	return os.platform();
}
