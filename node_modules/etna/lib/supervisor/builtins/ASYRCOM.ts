import { _ } from 'streamline-runtime';
import * as runtimeUtil from '../../engine/runtime/util';

module.exports = {
	TRACE: {
		params: ['MESSAGE'],
		body(_: _) {
			const frame = runtimeUtil.currentFrame();
			const message = frame.values['MESSAGE'];
			console.log(message.toString());
		}
	},
	SYRSND: {
		params: ['ASND'],
		body(_: _) {
			const frame = runtimeUtil.currentFrame();
			const asnd = frame.values['ASND'];
			asnd.$send(_);
		}
	}
};