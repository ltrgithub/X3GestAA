#!/usr/bin/env node --harmony

try {
	require("etna/lib/engine/register");
	var glob = require('streamline-runtime').globals;
	var arg = process.argv[2];
	var path = require('path');
	var p = path.join(process.cwd(), arg);
	require("etna/lib/engine/runtime/variables").initStack({
		engineErrMess: [],
	});
	var main = require(p).MAIN;
	main(function(err) {
		if (err) throw err;
	});
}
catch (ex) {
	console.log("failed: " + ex.stack);
}