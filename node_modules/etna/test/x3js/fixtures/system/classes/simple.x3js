import * as x3js from 'x3js';

export default x3js.class({
  name: 'Simple',
  module: 'system',
  tableDef: {
    name: 'UT_SIMPLE',
    isTemporary: true,
    indexes: [{
      columns: { 'ID': 1 },
    }],
  },
  properties: {
    // id
    id: {
      type: 'integer',
    },
    // numeric types
    mandatoryString: {
      type: 'string',
      maxLength: 10,
      isMandatory: true,
    },
  },
});