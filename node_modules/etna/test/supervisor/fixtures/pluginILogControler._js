"use strict";
var helpers = require('syracuse-core').helpers;
var flows = require('streamline-runtime').flows;


//

exports.PluginController = helpers.defineClass((_) => {

}, null, {
	callNew(_, opt, callBack) { // simulate a create of plugin and comm with ilog plugin and call the cb to confirm it's ok
		flows.setTimeout((_) => {
			callBack(_, {});
		}, 5000);

	},
	callUpdate(_, opt) {
		return {
			"test": "test2"
		};
	}
});