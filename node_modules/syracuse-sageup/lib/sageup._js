"use strict";

var sdataClient = require('./client');


exports.getAuthentication = function(id, emailSso, identifierType, identifierValue, typeTiers){
    return id+";"+emailSso+";"+identifierType+";"+identifierValue+";"+typeTiers;
}

exports.create = function(_, options){
	return sdataClient.create(_, options);
}