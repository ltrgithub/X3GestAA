"use strict";

module.exports = {
	application: "syracuse",
	extends: "collaboration",
	resources: require('./strings').module,
	entities: {
		batchServer: require('./entities/batchServer').entity,
		batchServerConfig: require('./entities/batchServerConfig').entity
	},
	representations: {},
	dbMeta: {
		initScript: [],
		updateScript: [{
			script: __dirname + '/updateScripts/updateScript',
			version: 1
		}]
	}
};