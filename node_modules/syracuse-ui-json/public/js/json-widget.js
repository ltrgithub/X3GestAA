"use strict";

var jsonTree = require('./jsonTree');

exports.create = function(opts) {
	return {
		setValue: function(val) {
			if (opts.facet === '$details') jsonTree.create(opts.div).setValue(val);
			else $('<span>').text('<JSON data>').appendTo($(opts.div));
		},
	};
};