"use strict";

exports.contract = {
	application: "syracuse",
	contract: "studio",
	// resources: require('./strings').module,
	entities: {
		studioSessionInfo: require('./entities/studioSessionInfo').entity,
	},
	representations: {},
	dbMeta: {
		initScript: [],
		updateScript: [{
			script: __dirname + '/updateScripts/updateScript',
			version: 2
		}]
	}
};