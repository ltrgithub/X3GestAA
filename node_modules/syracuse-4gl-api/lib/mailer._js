"use strict";

var mailer = require("syracuse-email/lib/mailer");

exports.$exported = true;

//application developments need user profile informations
exports.send = function(_, data, options) {
	mailer.send(_, data, options);
};