"use strict";
var registry = require("syracuse-sdata/lib/sdataRegistry");

//var strings = require('./strings');

exports.contract = {
	application: "demoapp",
	contract: "signature",
	//strings: strings, // necessary to be able to extend contract resources
	//resources: strings.resources,
	entities: {
		signer: require('./entities/signer').entity,
		contract: require('./entities/contract').entity,
	},
	representations: {
		signer: require('./representations/signer').representation,
		contract: require('./representations/contract').representation,
	},
	searchFacets: {
	},
	dbMeta: {
	}
};
