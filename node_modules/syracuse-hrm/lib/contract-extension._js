"use strict";

module.exports = {
	application: "syracuse",
	extends: "collaboration",
	resources: require('./strings').module,
	entities: {
		hrmSite: require('./entities/hrm/hrmSite').entity,
		hrmServer: require('./entities/hrm/hrmServer').entity,
	},
	representations: {},
	dbMeta: {
		initScript: [],
		updateScript: [{
			script: __dirname + '/updateScripts/updateScript',
			version: 1
		}]
	}
};