"use strict";

module.exports = {
	application: "syracuse",
	extends: "collaboration",
	resources: require('./strings').module,
	entities: {
		hrmSite: require('./entities/hrmSite').entity,
		hrmServer: require('./entities/hrmServer').entity,
		hrmTask: require('./entities/hrmTask/hrmTask').entity
	},
	representations: {
		hrmTask: require('./representations/hrmTask/hrmTask').representation
	},
	dbMeta: {
		initScript: [],
		updateScript: [{
			script: __dirname + '/updateScripts/updateScript',
			version: 1
		}]
	}
};