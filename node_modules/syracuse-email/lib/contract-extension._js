"use strict";

module.exports = {
	application: "syracuse",
	extends: "collaboration",
	resources: require('./strings').module,
	entities: {
		notificationEvent: require('./entities/notificationEvent').entity,
		notificationServer: require('./entities/notificationServer').entity,
		notificationServerOption: require('./entities/notificationServer').optEntity,
		notificationTheme: require('./entities/notificationTheme').entity,
		notificationThemeImage: require('./entities/notificationTheme').imgEntity,
	},
	representations: {
		notificationEvent: require('./representations/notificationEvent').representation
	},
	dbMeta: {
		initScript: [],
		updateScript: [{
			script: __dirname + '/updateScripts/updateScript',
			version: 1
		}]
	}
};