"use strict";
var config = require('./config');
var fsp = require('path');

config.streamline = config.streamline || {
	fibers: true,
	cache: true,
};

config.staticRoot = config.staticRoot || fsp.join(__dirname, '../uml-designer'),
config.dataRoot = config.dataRoot || fsp.join(__dirname, '../edi-runtime'),


require('streamline').register(config.streamline);
require('./lib/server');