"use strict";

/* jshint -W079 */
/* jshint unused: false */
/* global QUnit: false, asyncTest: false, test: false, strictEqual: false, start: false, stop: false */
var helpers = require('syracuse-core/lib/helpers');
var nodeconfig = require('syracuse-main/lib/nodeconfig');
var flows = require('streamline/lib/util/flows');
var SadFsqFile = require('syracuse-adxadmin/lib/sadfsq/sadfsqFile').SadFsqFile;
var fs = require('fs');

var SadFsqClient = require('syracuse-adxadmin/lib/sadfsq/sadfsqClient').SadFsqClient;
var util = require('util');

var done;
QUnit.module(module.id, {
	setup: function() {},
	teardown: function() {
		if (done) {
			setTimeout(function() {
				process.kill(process.pid);
			}, 500);
		}
	}
});

var client;
asyncTest("Client class instanciation", function(_) {
	//client = new SadFsqClient(_, "sodaix02", 17000, "apisu", "CRYPT:xkvvaTccvr", true);
	//client = new SadFsqClient(_, "ecchambard-001", 1818, "sage", "sage", true,{recmode:"REC",fileName:"test1"});
	client = new SadFsqClient(_, "ecchambard-001", 1818, "sage", "sage", true, {
		recMode: "PLAY",
		fileName: "test1"
	});
	var options = {
		path: "C:/",
		name: "AEXP1",
		extension: "json"
	};
	console.log("size " + JSON.stringify(client.getFileInfo(_, options, "size")));
	start();
});



test("kill", function() {
	done = true;
	start();
});