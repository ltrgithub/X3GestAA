"use strict";

const util = require('./util');
const helpers = require('syracuse-core/lib/helpers');
const tracer = require("syracuse-trace/lib/helper").getTracer("etna.engine");

class DBTableParent {
	constructor(driver, abbrev, superv, loc) {
		this.local = loc;
		this.superv = superv;
		this.driver = driver;
		this.abbrev = abbrev;
		this.slot = 0;
		this.types = {};
		this.values = {};
		this.inFrontOf = undefined;
	}

	init(_) {
		return this;
	}
}

exports.DBTableParent = DBTableParent;