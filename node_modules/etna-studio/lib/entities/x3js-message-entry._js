"use strict";
const entityHelper = require('etna/lib/x3js/entity-helper');
const messageDescriptor = require('etna/lib/x3js/message-descriptor').descriptor;
const x3jsUtil = require('etna/lib/x3js/util');

exports.entity = {
	$canDelete: false,
	$canCreate: false,
	$canEdit: false,
	$capabilities: "",
	$titleTemplate: "Message entry",
	$descriptionTemplate: " ",
	$valueTemplate: "{name}",
	$properties: {
		name: {
			$title: "name",
			$control: x3jsUtil.controlCamelCase('name'),
		},
		dictId: {
			$title: "id",
			$type: 'integer',
			$isReadOnly(_, instance) {
				return !!instance.dictId(_);
			},
		},
		title: {
			$title: "title",
			$isReadOnly(_, instance) {
				return !!instance.dictId(_);
			},
		},
		comment: {
			$title: "comment",
		},
	},
	$functions: {
		$toString(_) {
			var jsName = `${this._parent.name(_)}.${this.name(_)}`;
			const dictName = `${this._parent.dictId(_)}.${this.dictId(_)}`;
			return `message entry ${jsName} (${dictName})`;
		}
	},
};

entityHelper.fillEntity(exports.entity, messageDescriptor.collections.entries);