"use strict";
const entityHelper = require('etna/lib/x3js/entity-helper');
const classDescriptor = require('etna/lib/x3js/class-descriptor');
const x3jsUtil = require('etna/lib/x3js/util');

exports.entity = {
	$canDelete: false,
	$canCreate: false,
	$canEdit: false,
	$capabilities: "",
	$titleTemplate: "Class Behavior",
	$descriptionTemplate: " ",
	$valueTemplate: " ",
	$properties: {
		name: {},
	},
	$functions: {
		$toString(_) {
			return `behavior ${this._parent.name(_)} (${this.fromAlias(_)})`;
		}
	},
	$relations: {},
};

entityHelper.fillEntity(exports.entity, classDescriptor.collections.behaviors);