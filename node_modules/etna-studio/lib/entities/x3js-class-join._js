"use strict";
const entityHelper = require('etna/lib/api/entity-helper');
const classDescriptor = require('etna/lib/api/class-descriptor').descriptor;
const x3jsUtil = require('etna/lib/api/util');

exports.entity = {
	$canDelete: false,
	$canCreate: false,
	$canEdit: false,
	$capabilities: "",
	$titleTemplate: "Class Join",
	$descriptionTemplate: " ",
	$valueTemplate: " ",
	$properties: {},
	$functions: {
		$toString(_) {
			return `join ${this._parent.name(_)} (${this.fromAlias(_)})`;
		}
	},
	$relations: {},
};

entityHelper.fillEntity(exports.entity, classDescriptor.collections.joins);