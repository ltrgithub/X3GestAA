"use strict";
const entityHelper = require('etna/lib/api/entity-helper');
const classDescriptor = require('etna/lib/api/class-descriptor').descriptor;
const x3jsUtil = require('etna/lib/api/util');

exports.entity = {
	$canDelete: false,
	$canCreate: false,
	$canEdit: false,
	$capabilities: "",
	plural: 'x3jsClassTableIndexes',
	$titleTemplate: "Class Table Index",
	$descriptionTemplate: " ",
	$valueTemplate: " ",
	$properties: {},
	$functions: {
		$toString(_) {
			return `index ${this._parent.name(_)}.${this.name(_)}`;
		}
	},
	$relations: {},
};

entityHelper.fillEntity(exports.entity, classDescriptor.collections.tableDef.collections.indexes);