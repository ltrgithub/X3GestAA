"use strict";
const globals = require('streamline-runtime').globals;
const fsp = require('path');
const fs = require('fs');
const util = require('etna/lib/supervisor/util');
const x3jsUtil = require('etna/lib/x3js/util');
const entityParser = require('etna/lib/x3js/entity-parser');
const entityHelper = require('etna/lib/x3js/entity-helper');
const classDescriptor = require('etna/lib/x3js/classDescriptor');
const codeGen = require('etna/lib/x3js/entity-codegen');
const ezm = require('ez-mongodb');

function fillInstance(_, superv, instance, metaData, fileData) {
	if (fileData) {
		if (!fileData.module) throw new Error(`${fileData.file}: no module`);
		if (!fileData.properties) throw new Error(`${fileData.file}: no properties`);
		instance.module(_, fileData.module);
		instance.name(_, fileData.name);
		instance.dictCode(_, fileData.dictCode || '');
		instance.title(_, fileData.title || '');
		instance.status(_, fileData.status || '4gl');
		instance.comment(_, fileData.comment || '');
		instance.source(_, fs.readFile(fileData.file, 'utf8', _));
		const properties = instance.properties(_);
		Object.keys(fileData.properties).forEach_(_, (_, k) => {
			const fileProp = fileData.properties[k];
			const prop = properties.add(_);
			prop.name(_, k);
			prop.title(_, fileProp.title || '');
			prop.dictCode(_, fileProp.dictCode || '');
			prop.comment(_, fileProp.comment || '');
		});
	} else {
		instance.name(_, '');
		instance.status(_, '4gl');
		instance.comment(_, '');
		instance.source(_, '');
		entityHelper.fillFromMeta(_, instance, classDescriptor, superv, metaData);
	}
}

function updateAll(_, instance, saving) {
	if (instance.name(_) === '') {
		instance.status(_, '4gl');
	} else if (instance.status(_) === '4gl') {
		instance.status(_, 'incomplete');
	}
	if (instance.status(_) === '4gl') {
		instance.source(_, `// ${instance.dictCode(_)}: source not available. X3JS class name has not been set.`);
		// do not save to file.
		return;
	}

	// don't go further when synchronizing
	if (globals.context.dontGenerateX3jsSource) return;

	const superv = entityHelper.getSupervisor(_, instance.endpoint(_));
	const filePath = entityHelper.sourceFilename(_, instance, 'classes', superv);

	const missing = instance.missingMappings(_);
	const conflicts = instance.conflictingMappings(_);
	if (missing.length > 0 || conflicts.length > 0) instance.status(_, 'incomplete');
	if (saving) conflicts.forEach(conflict => instance.$addError(conflict));

	var parsed;
	if (fs.exists(filePath, _, _)) {
		parsed = entityParser.parseFile(_, filePath, classDescriptor);
	} else {
		const source = codeGen.template(_, classDescriptor);
		parsed = entityParser.parseSource(_, source, filePath, classDescriptor);
	}
	parsed.data = entityHelper.mergeData(parsed.data, classDescriptor, instance.serializeInstance(_), '');
	const output = codeGen.generateSourceCode(_, parsed, classDescriptor);
	var source = output.code;
	if (instance.status(_) === 'incomplete') {
		source = `// ${instance.name(_)}: source is incomplete and cannot be saved to file. Following mapping information is missing: ${
			'\n' + missing.concat(conflicts).map(s => '// * ' + s).join('\n') + '\n\n'
		}` + source;
	}
	instance.source(_, source);

	if (saving && instance.status(_) !== 'incomplete') codeGen.generateSourceFile(_, parsed.file, source);
}

exports.entity = {
	$canDelete: true,
	$canCreate: true,
	$canEdit: true,
	$capabilities: "",
	$titleTemplate: "Class {name} {title}",
	$listTitle: "List of X3JS classes",
	$descriptionTemplate: " ",
	$valueTemplate: " ",
	$properties: {
		module: {
			$title: "module",
			$isReadOnly: classDescriptor.rules.readOnlyProperty,
		},
		name: {
			$title: "X3JS name",
			$linksToDetails: true,
			$control: x3jsUtil.controlPascalCase('name'),
			$propagate: function(_, instance, value) {
				updateAll(_, instance);
			},
		},
		dictCode: {
			$title: "dictionary code",
			$linksToDetails: true,
			$isReadOnly: classDescriptor.rules.readOnlyProperty,
		},
		title: {
			$title: "title",
			$isReadOnly: classDescriptor.rules.readOnlyProperty,
		},
		status: {
			$title: "dev status",
			$enum: [{
				$value: "4gl",
				$title: "4GL",
			}, {
				$value: "incomplete",
				$title: "mapping in progress",
			}, {
				$value: "mapped",
				$title: "mapped",
			}, {
				$value: "x3js",
				$title: "X3JS",
			}, {
				$value: "error",
				$title: "error",
			}],
			$isReadOnly: true,
		},
		comment: {
			$title: "comment",
		},
		source: {
			$title: "source",
			$type: "javascript",
		},
	},
	$relations: {
		endpoint: {
			$type: "endPoint",
			$title: "Endpoint",
			$isReadOnly: true,
		},
		properties: {
			$type: "x3jsProperties",
			$isChild: true,
			$title: "Properties",
		},
	},
	$events: {
		$beforeSave(_, instance) {
			updateAll(_, instance, true);
		},
	},
	$functions: {
		$toString(_) {
			return `class ${this.name(_)} (${this.dictCode(_)})`;
		},
		updateSource(_) {
			updateAll(_, this);
		},
		conflictingMappings(_) {
			const names = {};
			const properties = this.properties(_).toArray(_);
			const errors = [];
			properties.forEach_(_, (_, prop) => {
				const name = prop.name(_);
				if (name) {
					if (names[name]) {
						errors.push(`duplicate property mapping: ${name} used by ${names[name].dictCode(_)} and ${prop.dictCode(_)}`);
					} else {
						names[name] = prop;
					}
				}
			});
			return errors;
		},
		missingMappings(_) {
			if (!this.name(_)) return [`${this.dictCode(_)}: missing X3JS class name`];
			const missing = [];
			this.properties(_).toArray(_).forEach_(_, (_, prop) => {
				if (!prop.name(_)) {
					missing.push(`${prop.dictCode(_)}: missing X3JS property name`);
				}
				if (prop.type(_) === 'enum' && /^\d/.test(prop.enum(_))) {
					missing.push(`${prop.dictCode(_)}: missing enum name - numeric enum id ${prop.enum(_)} must be converted to an enum name`);
				}
			});
			return missing;
		},
	},
	$services: {
		synchronize: {
			$method: "POST",
			$isMethod: false,
			$facets: ["$query"],
			$title: "Synchronize list",
			$invocationMode: "async",
			$execute(_, context) {
				const tracker = context.tracker;
				const userProfile = globals.context.session.getUserProfile(_);
				const endpoint = userProfile.selectedEndpoint(_);
				const superv = entityHelper.getSupervisor(_, endpoint);

				const parsedFiles = entityParser.parseSourceFiles(_, 'classes', classDescriptor, superv, tracker);
				const byDictCode = parsedFiles.reduce((r, parsed) => {
					if (parsed.data.dictCode) {
						const old = r[parsed.data.dictCode];
						if (old) throw new Error(`${parsed.file}: duplicate dictCode, already found in ${old.file}`);
						r[parsed.dictCode] = parsed.data;
					}
					return r;
				}, {});
				const classes = parsedFiles.map(parsed => parsed.data);

				const entity = context.db.model.getEntity(_, "x3jsClass");

				tracker.phase = 'processing metadata';
				const processed = {};
				ezm.reader(superv.mongoStore.collection('ACLASSE', _).find({})).forEach(_, (_, metaData) => {
					var oldData = context.db.db.collection('X3jsClass', _).find({
						'endpoint._uuid': endpoint.$uuid,
						dictCode: metaData.CODCLA,
					}).toArray(_)[0]

					// hack to avoid catastrophic performance and errors with propagate
					globals.context.dontGenerateX3jsSource = true;

					const instance = entity.factory.createInstance(_, null, context.db, null, oldData && oldData._id);
					instance.endpoint(_, endpoint);
					fillInstance(_, superv, instance, metaData, byDictCode[metaData.CODCLA]);
					// preserve mapping data.
					if (oldData) {
						instance.status(_, oldData.status);
						instance.name(_, oldData.name);
						instance.comment(_, oldData.comment);
						instance.properties(_).toArray(_).forEach_(_, (_, prop) => {
							const dictCode = prop.dictCode(_);
							oldData.properties.forEach_(_, (_, oldProp) => {
								if (oldProp.dictCode === dictCode) {
									prop.name(_, oldProp.name);
									prop.comment(_, oldProp.comment);
									return;
								}
							});
						});
					} else {
						instance.status(_, '4gl');
					}
					instance.save(_);
					tracker.$diagnoses = tracker.$diagnoses || [];
					if (instance.hasErrors(_)) {
						const oldLen = tracker.$diagnoses.length;
						instance.getAllDiagnoses(_, tracker.$diagnoses, {
							addEntityName: true,
							addPropName: true,
						});
						console.error("SAVE FAILED", tracker.$diagnoses.slice(oldLen));
					}
					processed[metaData.CODCLA] = true;
				});

				tracker.phase = 'purging metadata';
				context.db.db.collection('X3jsClass', _).remove({
					'endpoint._uuid': endpoint.$uuid,
					dictCode: {
						$not: {
							$in: Object.keys(processed),
						},
					},

				}, _);

				tracker.phase = 'inserting pure x3js classes';
				classes.forEach_(_, (_, fileData) => {
					if (fileData.dictCode) {
						if (processed[fileData.dictCode]) return;
						else throw new Error(`${fileData.name}: invalid dictCode: ${fileData.dictCode}`);
					}
					const instance = entity.factory.createInstance(_, null, context.db);
					instance.endpoint(_, endpoint);
					fillInstance(_, superv, instance, null, fileData);
					instance.save(_);
				});
				tracker.progress = 100;
				//return context.reply(_, 200, {});
			},
		},
	},
	$defaultOrder: [
		["name", true]
	]
};

entityHelper.addProperties(exports.entity, classDescriptor);