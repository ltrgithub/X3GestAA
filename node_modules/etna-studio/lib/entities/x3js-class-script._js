"use strict";
const entityHelper = require('etna/lib/api/entity-helper');
const classDescriptor = require('etna/lib/api/class-descriptor').descriptor;

exports.entity = {
	$canDelete: false,
	$canCreate: false,
	$canEdit: false,
	$capabilities: "",
	$titleTemplate: "Script",
	$descriptionTemplate: " ",
	$valueTemplate: " ",
	$functions: {
		$toString(_) {
			return `script ${this._parent.name(_)}.${this.name(_)}`;
		}
	},
	$relations: {},
};

entityHelper.fillEntity(exports.entity, classDescriptor.collections.scripts);