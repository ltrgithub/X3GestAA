"use strict";
const entityHelper = require('etna/lib/x3js/entity-helper');
const classDescriptor = require('etna/lib/x3js/classDescriptor');

exports.entity = {
	$canDelete: false,
	$canCreate: false,
	$canEdit: false,
	$capabilities: "",
	$titleTemplate: "Property",
	$descriptionTemplate: " ",
	$valueTemplate: " ",
	$properties: {
		name: {
			$title: "X3JS name",
		},
		title: {
			$title: "title",
			$isReadOnly(_, instance) {
				return !!instance.dictCode(_);
			},
		},
		dictCode: {
			$title: "dictionary code",
			$isReadOnly(_, instance) {
				return !!instance.dictCode(_);
			},
		},
		comment: {
			$title: "comment",
		},
	},
	$relations: {
		parameters: {
			$type: "x3jsPropertyParameters",
			$isChild: true,
			$title: "Parameters",
		},
	},
};

entityHelper.addProperties(exports.entity, classDescriptor.collections.properties);