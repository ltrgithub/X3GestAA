'use strict';
const util = require('etna/lib/supervisor/util');
const unitTestDescriptor = require('etna/lib/x3js/unit-test-descriptor');

function block(list) {
	return {
		$items: list.map(name => ({
			$bind: name
		}))
	};
}

exports.representation = {
	$entityName: 'x3jsUnitTest',
	$facets: {
		$details: {
			$layout: {
				$layoutType: 'tabs',
				$items: [{
					$category: 'section',
					$title: 'General',
					$layout: block(['module', 'name', 'title', 'active', 'comment', 'endpoint']),
				}, {
					$category: 'section',
					$title: 'Cases',
					$layout: block(['cases']),
				}, {
					$category: 'section',
					$title: 'Source',
					$layout: block(['source']),
				}]
			}
		},
		$edit: {
			$copy: '$details'
		},
	}
};
//console.error(JSON.stringify(exports.representation, null, 2));