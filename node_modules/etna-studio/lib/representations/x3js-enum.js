'use strict';
const util = require('etna/lib/supervisor/util');
const enumDescriptor = require('etna/lib/x3js/enum-descriptor');

function block(list) {
	return {
		$items: list.map(name => ({
			$bind: name
		}))
	};
}

exports.representation = {
	$entityName: 'x3jsEnum',
	$facets: {
		$details: {
			$layout: {
				$layoutType: 'tabs',
				$items: [{
					$category: 'section',
					$title: 'General',
					$layout: block(['module', 'name', 'dictId', 'status', 'title', 'comment', 'endpoint']),
				}, {
					$category: 'section',
					$title: 'Values',
					$layout: block(['entries']),
				}, {
					$category: 'section',
					$title: 'Source',
					$layout: block(['source']),
				}]
			}
		},
		$edit: {
			$copy: '$details'
		},
	}
};
//console.error(JSON.stringify(exports.representation, null, 2));