"use strict";

var globals = require('streamline/lib/globals');

exports.entity = {
	$titleTemplate: "Navigation",
	$valueTemplate: "{title}",
	$properties: {
		pageName: {
			$title: "Page name",
			$isUnique: true,
			$isMandatory: true,
			$pattern: "^[a-zA-Z0-9_]*$",
			$patternMessage: "Page name can only contain a to z, A to Z, 0 to 9 and _ caracters",
			$linksToDetails: true,
			$isDisabled: function(_, instance) {
				if (!instance.isFactory(_)) return false;
				var sp = globals.context.session && globals.context.session.getSecurityProfile(_);
				return (sp && sp.authoringLevel(_) && (sp.authoringLevel(_) !== "sage"));
			}
		},
		title: {
			$title: "Title",
			$isMandatory: true,
			$linksToDetails: true,
			$isLocalized: true
		},
		description: {
			$title: "Description",
			$isLocalized: true,
			$isHidden: true
		},
		isFactory: {
			$title: "Factory",
			$type: "boolean",
			$isDisabled: function(_, instance) {
				if (!instance.isFactory(_)) return false;
				var sp = globals.context.session && globals.context.session.getSecurityProfile(_);
				return (sp && sp.authoringLevel(_) && (sp.authoringLevel(_) !== "sage"));
			}
		}
	},
	$relations: {
		modules: {
			$title: "Modules",
			$type: "menuModules",
			$capabilities: "sort,reorder,delete",
			$inv: "navigationPages",
			$nullOnDelete: true
		},
		roles: {
			$title: "Roles",
			$type: "roles",
			$inv: "navigationPage",
			$isComputed: true,
			$nullOnDelete: true
		}
	},
	$links: function(_, instance) {
		var res = {};
		var sp = globals.context.session && globals.context.session.getSecurityProfile(_);
		if (!instance.isFactory(_) || !sp || !sp.authoringLevel(_) || (sp.authoringLevel(_) === "sage")) res.admin = {
			"$title": "Edit page content",
			"$url": "?representation={pageName}.$navigation_edit",
			"$method": "GET"
		};
		res.display = {
			"$title": "Preview",
			"$url": "?representation={pageName}.$navigation",
			"$method": "GET"
		};
		return res;
	},
	$events: {
		$afterSave: [

			function(_) {
				globals.context.session && globals.context.session.resetCache && globals.context.session.resetCache("navigationPage");
			}
		]
	},
	$searchIndex: {
		$fields: ["pageName", "title", "description", "modules"]
	},
	$exportProfile: {
		$key: "pageName",
		$properties: ["title", "description", "isFactory"],
		$relations: {
			modules: {
				$key: "code",
				$properties: ["title", "description", "isFactory"],
				$relations: {
					application: {
						$key: ["application", "contract"]
					},
					submodules: {
						$key: "code",
						$properties: ["title", "description", "isFactory"],
						$relations: {
							application: {
								$key: ["application", "contract"]
							},
							items: {
								$variants: {
									menuBlock: {
										$key: "code",
										$id: "menuBlock",
										$properties: ["code", "title", "isFactory"],
										$relations: {
											items: {
												$variants: {
													menuBlock: {
														$type: "pointer",
														$id: "menuBlock"
													},
													menuItem: {
														$key: "code",
														$properties: ["code", "title", "description", "linkType", "icon", "applicationMenu",
															"facet", "fusionFunction", "fusionKey", "keyParameter", "externalUrl",
															"dashboard", "target", "isFactory", "processName", "processMenu", "processLeg",
															"statName", "requestName", "requestLevel"
														],
														$relations: {
															application: {
																$key: ["application", "contract"]
															},
															endpoint: {
																$key: "dataset"
															},
															representationRef: {
																$properties: ["entity", "representation"]
															},
															module: {
																$key: "code"
															},
															parameters: {
																$properties: ["name", "title", "prompt", "value"]
															}
														}
													}
												}
											}
										}
									},
									menuItem: {
										$key: "code",
										$properties: ["code", "title", "description", "linkType", "icon", "applicationMenu",
											"facet", "fusionFunction", "fusionKey", "keyParameter", "externalUrl",
											"dashboard", "target", "isFactory", "processName", "processMenu", "processLeg",
											"statName", "requestName", "requestLevel"
										],
										$relations: {
											application: {
												$key: ["application", "contract"]
											},
											endpoint: {
												$key: "dataset"
											},
											representationRef: {
												$properties: ["entity", "representation"]
											},
											module: {
												$key: "code"
											},
											parameters: {
												$properties: ["name", "title", "prompt", "value"]
											}
										}
									}
								}
							}
						}
					}
				}
			}
		}
	}
};