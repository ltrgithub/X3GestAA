"use strict";
var boRestCli = require("syracuse-x3/lib/boRestCli").boRestCli;


exports.entity = {
	$titleTemplate: "Business Objects Server",
	$descriptionTemplate: "Configure Business Objects server",
	$valueTemplate: "{name}",
	$properties: {
		name: {
			$title: "Name",
			$isMandatory: true,
			$linksToDetails: true,
			$isUnique: true
		},
		host: {
			$title: "Host",
			$isMandatory: true
		},
		portTomcat: {
			$title: "Tomcat port",
			$isMandatory: true,
			$default: "8080"
		},
		portRest: {
			$title: "Web services port",
			$isMandatory: true,
			$default: "6405"
		}
	},
	$relations: {},
	$functions: {
		getBoServerBaseUrl: function(_, secure) {
			return (secure ? "https://" : "http://") + [this.host(_) + ":" + this.portTomcat(_)].join("/");
		},
	},
	$services: {}
};