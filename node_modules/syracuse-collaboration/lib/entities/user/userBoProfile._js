"use strict";

exports.entity = {
	$properties: {

	},
	$relations: {
		boServer: {
			$isUnique: true,
			$type: "boServer",
			$isMandatory: true,
		},
		profile: {
			$type: "boProfile",
			$title: "Profile",
			//			$lookup: {
			//				field: "name",
			//				parameters: "boServer={boServer}"
			//			},
			//
			$select: {
				$title: "Profiles",
				$type: "lookupBoProfile",
				$fieldMap: {
					name: "name"
				},
				$parameters: "boServer={boServer}"
			},

			$isHidden: function(_, instance) {
				var boServer = instance.boServer(_);
				return boServer == null;
			},
			$isMandatory: true
		}
	}
};