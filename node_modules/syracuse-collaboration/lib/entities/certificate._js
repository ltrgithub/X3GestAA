"use strict";
var locale = require("syracuse-core/lib/locale");
var datetime = require("syracuse-core/lib/types/datetime");
var util = require('util');
var config = require('syracuse-main/lib/nodeconfig').config;
var Certificate = require("jsx509").Certificate;

/// fillInstance: reads PEM certificate information and fills in the distinguished name of issuer and subject to the instance
function fillInstance(instance, pemInput, _) {
	if (pemInput.indexOf('CERTIFICATE---') > 0) {
		var b64 = pemInput.substring(pemInput.indexOf('ATE-----') + 8, pemInput.indexOf('-----END'));
		var certificate = new Buffer(b64, "base64");
		var cert = new Certificate(certificate);
		instance.dn(_, _getDn(cert.subject));
		instance.issuerDn(_, _getDn(cert.issuer));
		instance.notBefore(_, new datetime.Datetime(cert.notBefore))
		instance.notAfter(_, new datetime.Datetime(cert.notAfter))
	} else {
		throw new Error(locale.format(module, "noCert"));
	}
}
exports.fillInstance = fillInstance;

// make distinguished name out of data in object
function _getDn(info) {
	var result = "C="+info.countryName+(info.stateOrProvinceName ? ", ST="+info.stateOrProvinceName : "")+
		(info.localityName ? ", L="+info.localityName : "")+", O="+info.organizationName+", ";
	info.organizationalUnitNames.forEach(function(ou) {
		result += "OU="+ou+", ";
	})
	return result +"CN="+info.commonName;		
}

exports.entity = {
		$properties: {
			name: {
				$title: "Name",
				$isMandatory: true,
				$isUnique: true
			},
			certificate: {
				$title: "Certificate",
				$type: "binary",
				$storage: "db_file",
				$isMandatory: true
			},
			key: {
				$title: "Private key",
				$type: "binary",
				$storage: "db_file" 
			},
			pass: {
				$title: "Passphrase"
			},
			dn: {
				$title: "Distinguished name",
				$isReadOnly: true
			},
			issuerDn: {
				$title: "Issuer distinguished name",
				$isReadOnly: true
			},
			notBefore: {
				$title: "Valid from",
				$isReadOnly: true,
				$type: "datetime"
			},
			notAfter: {
				$title: "Valid until",
				$isReadOnly: true,
				$type: "datetime"
			}
		},
		$titleTemplate: "Certificates",
		$valueTemplate: "{name}",
		$events: {
			$beforeSave: [function(_, instance) {
				var cl = instance.certificate(_);
				if (cl.fileExists(_)) {
					var pemInput = cl.createReadableStream(_).read(_, -1).toString();
					fillInstance(instance, pemInput, _);
				}
			}],
			$afterSave: [function(_, instance) { // update nanny processes unless special marker property has been set
				if (config.mockServer) {
					var options = { path: "/nannyCommand/notifyNannies/update", method: "PUT", hostname: "", port: 0};
					try {
						console.log(mock.simpleRequest(config.mockServer.mockClient, options, null, _));			
					} catch (e) {
						console.log("Error "+e);
					}
				}
			}]
		},
		$searchIndex: {
			$fields: ["name"]
		},
		$functions: {
		},
		$defaultOrder: [["name", true]],
		$services: {
		},
		$relations: {
			caCertificates: {
				$title: "CA Certificates",
				$type: "caCertificates",
				$inv: "certificates"
			},

		},
		$actions: {
			$save: function(_, instance) {
				var r = {};
				if (config.mockServer && instance.$snapshot && !instance.$created) r.$confirm = locale.format(module, "maybeRestart")
				return r;
			}
		}
}