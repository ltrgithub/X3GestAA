"use strict";

var sys = require("util");
var adminHelper = require("syracuse-collaboration/lib/helpers").AdminHelper;
var t9n = require("syracuse-translation/lib/translation");
var TranslationProcess = t9n.TranslationProcess;
var jsurl = require("jsurl");
var helpers = require("syracuse-core/lib/helpers");

var fs = require('streamline-fs');
var fsp = require("path");
var flows = require('streamline/lib/util/flows');

var locked = false;

exports.entity = {
	$isPersistent: false,
	$canSave: false,

	$titleTemplate: "Translation",
	$descriptionTemplate: "Translation of syracuse messages and administration data",
	$valueTemplate: "{description}",
	$properties: {
		// conflictPolicy: {
		// 	$title: "Conflict policy",
		// 	$enum: [{
		// 		$value: "none",
		// 		$title: "None"
		// 	}, {
		// 		$value: "source",
		// 		$title: "Resolve using source"
		// 	}, {
		// 		$value: "target",
		// 		$title: "Resolve using target"
		// 	}, {
		// 		$value: "index",
		// 		$title: "Resolve using index"
		// 	}],
		// 	$default: "none"
		// },
		syracuse: {
			$title: "Syracuse resources",
			$type: "boolean",
			$default: true
		},
		dotnet: {
			$title: "DotNet resources",
			$type: "boolean",
			$default: true
		},
		admin: {
			$title: "Admin resources",
			$type: "boolean",
			$default: false
		},
		landingPageTabOnly: {
			$title: "Landing Page tab only",
			$type: "boolean",
			$default: false
		},
		resetIndex: {
			$title: "Reset index (extract only)",
			$type: "boolean"
		},
		ignoreIndexEndpoint: {
			$title: "Ignore index endpoint",
			$type: "boolean",
			$default: true
			// $isHidden:false
		},
		ignoreChecksum: {
			$title: "Ignore checksum",
			$type: "boolean",
			$default: true
			// $isHidden:false
		},
		loadFromIndex: {
			$title: "Load from index (reinject only)",
			$type: "boolean",
			$isHidden: false
		},
		noDBStore: {
			$title: "No DB Store",
			$type: "boolean",
			$default: true
			// $isHidden:false
		},
		verify: {
			$title: "Verify (extract only)",
			$type: "boolean"
		},
		// filename: {
		// 	$title: "File name",
		// 	$type: "string"
		// },
		// log: {
		// 	$title: "Log",
		// 	// $type: "binary",
		// 	// $storage: "file",
		// 	// $isReadOnly: true,
		// 	$default: "...",
		// 	$compute: function(_, instance) {
		// 		console.log("$compute: " + instance.__data);
		// 		return instance.__data || "...";
		// 	}
		// }
	},
	$relations: {
		endpoint: {
			$title: "Production environnement endpoint",
			$description: "Select the endpoint used as reference for translation teams",
			$type: "endPoint",
			// $isMandatory: true,
			$isDefined: true,
			$lookupFilter: {
				// for testing purposes, comment filter
				//protocol: "x3"
			}
		}
	},
	$services: {
		downloadLog: {
			$method: "GET",
			$isMethod: true,
			$title: "Download log",
			$type: "application/x-file",
			$execute: function(_, context, instance) {
				return {
					headers: {
						"content-type": "text/plain"
					},
					body: fs.readFile(t9n.getLogFile(), "utf8", _)
				};
			}
		},

		fromSyracuse: {
			$method: "POST",
			$isMethod: true,
			$title: "Extract from Syracuse",
			$invocationMode: "async",
			$isDisabled: function(_, instance) {
				return (!instance.noDBStore(_) && !instance.endpoint(_)) || locked;
			},
			$execute: function(_, context, instance, parameters) {
				try {
					instance.$diagnoses = instance.$diagnoses || [];
					if (locked) {
						throw new Error("Translation process is in progress on another session");
					}
					locked = true;
					var translation = new TranslationProcess(_, context, makeOptions(_, context, instance, parameters));
					translation.extractAll(_);
					// p.init(_, fsp.join(config.system.root, "translation-indexes", "translation.log"));
				} catch (e) {
					instance.$diagnoses.push({
						$severity: "error",
						$message: e.message || e
					});
				}
				locked = false;
			}
		},

		updateIndexfromCsv: {
			$method: "POST",
			$isMethod: true,
			$title: "Update index from csv",
			$invocationMode: "async",
			$isDisabled: function(_, instance) {
				return (!instance.noDBStore(_) && !instance.endpoint(_)) || locked;
			},
			$execute: function(_, context, instance, parameters) {
				try {
					instance.$diagnoses = instance.$diagnoses || [];
					if (locked) {
						throw new Error("Translation process is in progress on another session");
					}
					locked = true;
					var translation = new TranslationProcess(_, context, makeOptions(_, context, instance, parameters));
					translation.updateIndexfromCsv(_);
					// p.init(_, fsp.join(config.system.root, "translation-indexes", "translation.log"));
				} catch (e) {
					instance.$diagnoses.push({
						$severity: "error",
						$message: e.message || e
					});
				}
				locked = false;
			}
		},

		toSyracuse: {
			$method: "POST",
			$isMethod: true,
			$title: "Reinject to Syracuse",
			$invocationMode: "async",
			$isDisabled: function(_, instance) {
				return (!instance.noDBStore(_) && !instance.endpoint(_)) || locked;
			},
			$execute: function(_, context, instance, parameters) {
				try {
					instance.$diagnoses = instance.$diagnoses || [];
					if (locked) {
						throw new Error("Translation process is in progress on another session");
					}
					locked = true;
					var translation = new TranslationProcess(_, context, makeOptions(_, context, instance, parameters));
					translation.updateAll(_);
				} catch (e) {
					console.log("exception: " + (e.message || e) + "\n" + e.stack);
					instance.$diagnoses.push({
						$severity: "error",
						$message: e.message || e,
						$stackTrace: e.safeStack
					});
				}
				locked = false;
			}
		},

		deleteAll: {
			$method: "POST",
			$isMethod: true,
			$title: "Delete all messages",
			$invocationMode: "async",
			$isDisabled: function(_, instance) {
				return (!instance.noDBStore(_) && !instance.endpoint(_)) || locked;
			},
			$execute: function(_, context, instance, parameters) {
				t9n.deleteAll(_, makeOptions(_, context, instance, parameters));
			}
		},

		transformResourceFile: {
			$method: "POST",
			$isMethod: true,
			$title: "Transform mobile resources",
			$isDisabled: function(_, instance) {
				return false;
			},
			$execute: function(_) {
				var fpath = fsp.join(__dirname, "../../../syracuse-mobile/html/mobile/js/resources", "mobile-en.json");
				var newfpath = fsp.join(__dirname, "../../../syracuse-mobile/html/mobile/js/resources", "mobile1-en.json");

				function addEl(_, key, content) {
					console.log("addEl key: " + key + " content: " + content);
					keys.push(key);
					console.log("keys*********: " + JSON.stringify(keys, null, 2));

					if (typeof content === "string") {
						var newkey = keys.join('.');
						result[newkey] = content;
					} else if (typeof content === "object" && !content.length) {
						flows.eachKey(_, content, addEl);
					} else if (typeof content === "object" && content.length) {
						content.forEach_(_, function(_, el, idx) {
							addEl(_, el, el);
						});
					}
					keys.pop();
				}

				var result = {},
					keys = [],
					input, parsed;
				try {
					input = fs.readFile(fpath, "utf8", _);
				} catch (ex) {
					console.log("can not read file:  " + fpath);
				}

				try {
					parsed = JSON.parse(input);
				} catch (e) {
					console.log("incorrectJsonFormat ");
					return;
				}
				flows.eachKey(_, parsed, addEl);

				fs.writeFile(newfpath, JSON.stringify(result, null, 2), "utf8", _);
			}
		}
	}
};

function makeOptions(_, context, instance, parameters) {
	var t = context && context.tracker,
		options = parameters || {};
	options.$tracking = t ? (t.$diagnoses = t.$diagnoses || []) : null;
	options.$addDiagnosis = instance.$addDiagnose;
	options.resources = {
		syracuse: instance.syracuse(_),
		dotnet: instance.dotnet(_),
		admin: instance.admin(_)
	};
	options.noDBStore = instance.noDBStore(_);
	options.endpoint = (options.noDBStore && adminHelper.getCollaborationEndpoint(_)) || instance.endpoint(_);
	options.resetIndex = instance.resetIndex(_);
	options.loadFromIndex = instance.loadFromIndex(_);
	options.ignoreIndexEndpoint = instance.ignoreIndexEndpoint(_);
	options.verify = instance.verify(_);
	options.ignoreChecksum = instance.ignoreChecksum(_);
	options.landingPageTabOnly = instance.landingPageTabOnly(_);
	return options;
}