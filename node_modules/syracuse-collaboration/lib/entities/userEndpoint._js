"use strict";

var sys = require("util");

exports.entity = {
	$properties: {
		login: {
			$title: "User Login",
			$isMandatory: true
		}
	},
	$titleTemplate: "{user.login}",
	$relations: {
		user: {
			$type: "user",
			$inv: "endpoints"
		},
		endpoint: {
			$type: "endPoint",
			$title: "Endpoint",
			$isMandatory: true
		}
	}
};