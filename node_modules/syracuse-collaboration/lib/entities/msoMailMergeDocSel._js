"use strict"

exports.entity = {
	$titleTemplate: "Create word document",
	$descriptionTemplate: " ",
	$valueTemplate: " ",
	
	$isPersistent: false,
	
	$properties: {
        creationMode: {
            $title: "Create document by",
            $enum: [{
				$value: "use_tpl",
				$title: "Use existing template"
			},{
				$value: "new_empty_doc",
				$title: "New empty document"
			},{
				$value: "new_word_tpl",
				$title: "New document by word template"
			}],
            $default: "use_tpl"
        },
		msoCurrentRepresentation: {
			$title: "Representation",
			$isDisabled: true
		}
	},
    $relations: {
    	document: {
			$title: "Document",
			$type: "document",
			$isDisabled: function(_, instance) {
                return instance.creationMode(_) !== "use_tpl";
			},
			$isMandatory: function(_, instance) {
				return instance.creationMode(_) === "use_tpl";
			},
			$lookupFilter: function(_, instance, filter, params) {
				return { 
					"$and": [{
						"internalTags.tagKey": "$templateClass",
						"internalTags.tagValue": instance.msoCurrentRepresentation(_)
					}, {
						"internalTags.tagKey": "$templateType",
						"internalTags.tagValue": "mailmerge",
					}]
				};
			} 
    	}
	}
}
