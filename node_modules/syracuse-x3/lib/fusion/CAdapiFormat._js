"use strict";
var helpers = require('syracuse-core/lib/helpers');
var CFormatStub = function() {};

var CAdapiFormat = helpers.defineClass(function(aTracer, aFormat) {
	TODO_SUPER(aTracer, CFormater.FORMATER_JAVA, aFormat);
}, CFormatStub, {
/* TODO overload
	public CAdapiFormat(aFormat) {
		this(CTracerNull.instance, aFormat);
	}
	*/
});

CAdapiFormat.newEmptyFormat = function() {
	return new CAdapiFormat(CFormatStub.EMPTY_DEF);
}