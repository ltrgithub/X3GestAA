"use strict";
var helpers = require('syracuse-core/lib/helpers');

var CAdapiBlob = helpers.defineClass(function(aBlobValue, mimeType) {
	this.blobValue = aBlobValue;
	this.mimeType = mimeType;
}, null, {
	// TODO: constructor overload
/*
	protected CAdapiBlob(aResu, mimeType) {
		this(aResu.getBytesValue(), mimeType != null ? mimeType : aResu.getValue());
	}
	*/

	getBlobValue: function() {
		return this.blobValue;
	},
	getMimeType: function() {
		return this.mimeType;
	}
});