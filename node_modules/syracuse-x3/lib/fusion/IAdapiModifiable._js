"use strict";
var helpers = require('syracuse-core/lib/helpers');

var StyleApply = {
	ENTITY: 0,
	CHILDS: 1,
	TITLE: 2
}

function getApply(arg) {
	var idx = arg == null ? 0 : arg.structIst.idx;
	switch (idx) {
	case -1:
		return CHILDS;
	case -2:
		return TITLE;
	default:
		return ENTITY;

	}
}