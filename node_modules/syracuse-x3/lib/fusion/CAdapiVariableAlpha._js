"use strict"; var helpers = require('syracuse-core/lib/helpers');

var CAdapiVariableAlpha = helpers.defineClass(function() {}, CAdapiVariable, {

	protected CAdapiVariableAlpha(srcLine, aEntityIndexBaseOne, aVarName, aID, aTdo, aFormat, aEntityState) {
		TODO_SUPER(srcLine, aEntityIndexBaseOne, aVarName, aID, aTdo, aFormat, aEntityState);
	}, clone: function() {
		var wVariable = new CAdapiVariableAlpha(getSrcLine(), getEntityIndexBaseOne(), getName(), getId(), getDataType(), getFormat(), getState());
		wVariable.setMustBeFormated(this.mustBeFormated());
		this.cloneValues(wVariable);
		return wVariable;
	}, unFormatData: function(aValue) {
		if (!this.mustBeFormated()) {
			return aValue;
		}
		if (aValue == null) {
			return null;
		}
		if (aValue.length() == 0) {
			return aValue;
		}
		return this.callUnFormater(aValue);
	}
}
