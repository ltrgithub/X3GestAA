"use strict";
var helpers = require('syracuse-core/lib/helpers');
var CAdapiListOfEntity = function() {};

var CAdapiListOfDataSrcs = helpers.defineClass(function(aAdapiRequesterWindow, aAdapiScreen) {
	TODO_SUPER(aAdapiRequesterWindow);
	this.adapiScreen = aAdapiScreen;
}, CAdapiListOfEntity, {
	traverseAll: function(aTraverser) {
		var wMax = this.size();
		var wAdapiDataSrc;
		var wI = 0;
		while (wI < wMax) {
			wAdapiDataSrc = this.get(wI);
			if (aTraverser.doWhenTraverse(wAdapiDataSrc, wI) == ITraverser.STOP_TRAVERSING) {
				return ITraverser.STOP_TRAVERSING;
			}
			if (wAdapiDataSrc.traverseAll(aTraverser) == ITraverser.STOP_TRAVERSING) {
				return ITraverser.STOP_TRAVERSING;
			}
			wI++;
		}
		return ITraverser.CONTINUE_TRAVERSING;
	},
	traverseChilds: function(aTraverser) {
		var wMax = this.size();
		var wEntry;
		var wI = 0;
		while (wI < wMax) {
			wEntry = this.get(wI);

			if (aTraverser.doWhenTraverse(wEntry, wI) == ITraverser.STOP_TRAVERSING) {
				return ITraverser.STOP_TRAVERSING;
			}
			wI++;
		}
		return ITraverser.CONTINUE_TRAVERSING;
	},
	traverseSettable: function(aStructIst, aAdapiSetter) {
		var wI = aStructIst.blocZeroBase;
		if (wI < 0 || wI >= this.size()) {
			var wMess = this.adapiClient.adapiResources.getMessage(CAdapiResources.KEYMESS_ERROR_DSRC_UNKNOWN);
			wMess = CStringFacilities.sprintf(wMess, String.valueOf(wI), String.valueOf(this.size()), this.adapiScreen != null ? this.adapiScreen.getAdxId() : "???");
			aAdapiSetter.setLogicalErrMess(wMess);
			return false;
		}
		var wAdapiDataSrc = this.get(wI);
		return wAdapiDataSrc.traverseSettable(aStructIst, aAdapiSetter);
	}
});