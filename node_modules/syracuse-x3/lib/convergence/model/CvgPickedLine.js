"use strict";
var helpers = require('syracuse-core/lib/helpers');
var CvgAction = require('syracuse-x3/lib/convergence/client/CvgAction').Actions;
var x3helpers = require('syracuse-x3/lib/helpers');

var localConfig = x3helpers.config("x3fusion", null, {});
var traceLog = x3helpers.config("log.trace", localConfig);

var CvgPickedLine = exports.CvgPickedLine = helpers.defineClass(function(aStructIst, key, leftList, aAction) {
	this.ist = aStructIst;
	this.key = key;
	this.action = aAction;
	this.leftList = leftList;
}, null, {
	isSelection: function() {
		traceLog && traceLog((new Date().toISOString()) + " action " + this.action);
		return this.action === CvgAction.SRV_TREESELECTLINE || this.action === CvgAction.SRV_BRWSELECTLINE;
	}
});