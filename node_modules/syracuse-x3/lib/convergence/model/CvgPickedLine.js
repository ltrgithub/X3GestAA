"use strict";
var helpers = require('syracuse-core/lib/helpers');

var tracerStd = require("syracuse-trace/lib/helper").getTracer("classic.std");

var CvgPickedLine = exports.CvgPickedLine = helpers.defineClass(function(client, aStructIst, key, leftList, aAction) {
	this.ist = aStructIst;
	this.key = key;
	this.action = aAction;
	this.leftList = leftList;
	this.client = client;
}, null, {
	isSelection: function() {
		tracerStd.info && tracerStd.info((new Date().toISOString()) + " action " + this.action);
		return this.action === this.client.x3writer.actions.SRV_TREESELECTLINE || this.action === this.client.x3writer.actions.SRV_BRWSELECTLINE;
	}
});