"use strict";
var helpers = require('syracuse-core/lib/helpers');
var CvgObservable = require('syracuse-x3/lib/convergence/model/CvgObservable').CvgObservable;

var CvgCallUiOpenFile = exports.CvgCallUiOpenFile = helpers.defineClass(function(client, type, url, appSrvShared) {
	CvgObservable.call(this, client);
	this.targetlist = [
		[type, url, appSrvShared]
	];

	this.setJsonType('MISC');
}, CvgObservable, {
	addOpenFile: function(type, url) {
		this.targetlist.push([type, url, appSrvShared]);
	},
	toJSON: function() {
		return {
			openf: {
				target: this.targetlist
			}
		};
	},
	toJsonObj: function() {
		return this.toJSON();
	}

});