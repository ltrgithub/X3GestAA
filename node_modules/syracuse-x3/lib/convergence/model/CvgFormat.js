"use strict";
var helpers = require('syracuse-core').helpers;

var CvgFormat = exports.CvgFormat = helpers.defineClass(function(format) {
	this.format = format && format.length === 1 && format[0] === ':' ? "" : format;
}, null, {
	isValid: function() {
		return this.format && this.format.length > 0;
	},
	isVisible: function() {
		return this.format && this.format.length > 0 && this.format[0] !== '-';
	},
	isLocalMenu: function() {
		return this.format && this.format.length > 0 && this.format[0] == 'L';
	},
	toString: function() {
		return "FORMAT: " + this.format;
	}
});