"use strict";

var locale = require('streamline-locale');

var done;
QUnit.module(module.id, {
	setup: function() {}
});



it('parse query to calendar  ', function(_) {

});


test("kill", function() {
	done = true;
});