"use strict";

var locale = require('streamline-locale');

var done;
QUnit.module(module.id, {
	setup: function() {}
});



asyncTest("parse query to calendar  ", function(_) {

	start();
});


test("kill", function() {
	done = true;
	start();
});