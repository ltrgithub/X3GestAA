"use strict";

var locale = require('streamline-locale');

var done;
describe(module.id, () => {



	it('parse query to calendar  ', function(_) {

	});


	it('kill', function() {
		done = true;
	});
});