"use strict";
var helpers = require('syracuse-core/lib/helpers');
var flows = require("streamline/lib/util/flows");


//

exports.PluginController = helpers.defineClass(function(_) {

}, null, {
	callNew: function(_, opt, callBack) { // simulate a create of plugin and comm with ilog plugin and call the cb to confirm it's ok
		flows.setTimeout(function(_) {
			callBack(_, {});
		}, 5000);

	},
	callUpdate: function(_, opt) {
		return {
			"test": "test2"
		};
	}
});