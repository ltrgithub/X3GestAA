"use strict";

var ez = require('ez-streams');

exports.newDevice = function(device) {
	switch (device) {
		// test with files
		case "file":
			return ez.devices.file.text.writer("d:\\__test");
		case "console":
		default:
			return ez.devices.console.log;
	}
};