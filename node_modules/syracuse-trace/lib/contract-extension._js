"use strict";

module.exports = {
	application: "syracuse",
	extends: "collaboration",
	resources: require('./strings').module,
	entities: {
		traceRecord: require('./entities/traceRecord').entity,
		traceContent: require('./entities/traceContent').entity,
		traceConfig: require('./entities/traceConfig').entity,
		traceConfigModule: require('./entities/traceConfigModule').entity,
		traceSetting: require('./entities/traceSetting').entity,
	},
	representations: {
		traceRecord: require('./representations/traceRecord').representation,
	},
	dbMeta: {
		initScript: [],
		updateScript: [{
			script: __dirname + '/updateScripts/updateScript',
			version: 1
		}]
	}
};