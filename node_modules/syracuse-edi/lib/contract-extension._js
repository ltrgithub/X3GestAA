"use strict";

module.exports = {
	application: "syracuse",
	extends: "collaboration",
	resources: require('./strings').module,
	entities: {
		ediCache: require('./entities/ediCache').entity,
		ediProcess: require('./entities/ediProcess').entity,
	},
	representations: {},
	dbMeta: {

	}
};