'use strict';

var path = require('path');
require('npm-shadow')();

//process.env.HOME = path.join(__dirname, "../../../..");

var apis = require('@sage/syracuse-core').apis;

apis.register('test-admin', function() {
	return require("@sage/syracuse-lib/test/collaboration/fixtures/adminTestFixtures");
});
apis.register('test-runner', function() {
	return {
		startServers: require("syracuse-main/lib/syracuse").startServers
	};
});

require('@sage/syracuse-lib/src/license/index').register(function(err) {
	if (err) throw err;
	require("@sage/syracuse-lib/src/license/check");
	//require("syracuse-main/lib/syracuse");
});