"use strict";

exports.tracer; // = console.log;
import * as config from 'config';
import * as adminHelpers from '../../collaboration/helpers';
var _scripts = [];

_scripts[1] = function(_, db) {
	exports.tracer && exports.tracer("Executing update script to version: 1; Add AutoStartDisabled");
	//
	var coll = db.db.collection("SoapClassicPool", _);
	var elts = coll.find({}).toArray(_);

	if (elts && elts.length > 0) {
		elts.forEach_(_, function(_, e) {
			exports.tracer && exports.tracer("Set AutoStartDisabled on Web Service Pool configuration " + e.name);

			// UPDATE
			coll.update({
				_id: e._id
			}, {
				$set: {
					_updDate: new Date(),
					autoStartDisabled: false,
				}
			}, {
				safe: true,
				multi: true
			}, _);

		});
	}
	exports.tracer && exports.tracer("Update script to version: 1 executed");
};



export function dataUpdate(_, db, actualVersion, targetVersion) {
	// force log: always
	exports.tracer = console.error;
	//
	_scripts.slice(actualVersion + 1, targetVersion + 1).forEach_(_, function(_, sequence) {
		sequence && sequence(_, db);
	});
};

export const metadata = {
	fileId: "aa961bfeb0e0", // this id MUST never change and MUST be unique over all update scripts
	description: "2017_R5 update script" // !important, some description, optional and can change
};