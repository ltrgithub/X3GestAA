"use strict";

export const entity = {
	$isPersistent: false,
	$properties: {
		schedulerId: {
			$isHidden: true
		}
	},
	$relations: {
		scheduler: {
			$title: "Scheduler",
			$type: "automate",
			$propagate(_, instance, val) {
				instance.schedulerId(_, val && val.$uuid);
			}
		}
	}
};