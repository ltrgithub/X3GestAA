"use strict";

export const entity = {
	$key: "{dataset}",
	$properties: {
		dataset: {
			$linksToDetails: true,
			$details: {
				$url: "{epBaseUrl}/ABOUT('A')?representation=ABOUT.$details",
				$target: "popup"
			}
		},
		description: {},
		epBaseUrl: {
			$isHidden: true
		}
	},
	$functions: {
		$setId(_, context, id) {
			console.log("About id is : " + id);
		}
	}
};