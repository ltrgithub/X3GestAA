"use strict";
import { SitemapModule, SitemapSubmodule } from '../../sitemap/groups';
import { SitemapMenu, SitemapBlock, IEntity } from '../../sitemap/items';

export interface IPath {
    module: SitemapModule,
    submodule: SitemapSubmodule,
    blocks: SitemapBlock[],
    menu: SitemapMenu
}
export interface IProps {
    onMenuClick: (menu: SitemapMenu, evet: MouseEvent) => void,
    onExpand: (entity: IEntity) => void,
    expandedList: any,
    path: IPath
}