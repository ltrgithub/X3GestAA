"use strict";

export interface ISyraFieldItem{
    $bind:string,
    $inplace : boolean,
    $isDiagnoseInline: boolean
}
export interface ISyraField{
    id: string,
    getTitle:() => string,
    $item:ISyraFieldItem,
    _core: HTMLElement,
    _dataValue: HTMLElement,
    refDescriptionItem: any,
    descriptionItem: any,
    $setValue:any,
    diagsSlot?:HTMLElement,
    isDiagnosePopup: boolean,
    $isEditMode:boolean,
    isDiagnoseDisabled:boolean   
};

export interface ISyraAlertMessage{
    autoHide:boolean,
    $bind?:string
    field?:ISyraField
    $diagnoses?:any[]
};