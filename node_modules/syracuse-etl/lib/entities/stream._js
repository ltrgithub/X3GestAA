"use strict";

var date = require("syracuse-core/lib/types/date");
var datetime = require("syracuse-core/lib/types/datetime");
var globals = require('streamline/lib/globals');
var scheduler = require("syracuse-event/lib/scheduler");
var config = require('config');

exports.entity = {
	$titleTemplate: "Technical Streams",
	$valueTemplate: "{description}",
	$descriptionTemplate: "Management of technical streams",
	$canCreate: false,
	$canDelete: true,
	$canEdit: false,
	$properties: {
		name: {
			$title: "Name",
			$isMandatory: true,
			$isReadOnly: true,
			$linksToDetails: true,
		},
		type: {
			$title: "Stream type",
			$isReadOnly: true
		},
		stamp: {
			$title: "Timestamp",
			$type: "datetime",
			$isReadOnly: true,
			$isNullable: true
		},
		content: {
			$title: "Content",
			$type: "binary",
			$isDisabled: false,
			$storage: "db_file",
		},
	},
	$relations: {},
	$searchIndex: {
		$fields: ["name", "type", "date"]
	},
	$functions: {},
	$services: {
		generateFlamegraph: require('syracuse-flamegraph').convertRecordingToSvg,
	},
};