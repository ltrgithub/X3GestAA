"use strict";
var module = QUnit.module;
//var convert = require("syracuse-rtf");
var rtfbuilder = require("syracuse-rtf/lib/buildrtf");
 //var rtfbuilder=require('syracuse-rtf/lib/toto');
var parser = require('syracuse-rtf/lib/rtfparse');
var interpret = require('syracuse-rtf/lib/rtfinterpret');
var build = require('syracuse-rtf/lib/buildhtml');
var rtf= require('syracuse-rtf/lib/rtf');
module("rtf parser");

function escape(html) {

    return html;//.replace(/\&/g, '&amp;').replace(/\</g, '&lt;').replace(/\>/g, '&gt;');
}

function testConvertHtml(html,result, name) {

   var rawtext=rtf.extractText(html,false) ;
   strictEqual(rawtext, result, name);
}

function testConvertRaw(rowtext, result, name) {

    var html = rtf.rawTextToHtml(rowtext);
    strictEqual(html, result, name);
}

test('basic', 21, function() {
    

testConvertHtml("<b>bold</b>", "bold", "simple tag ");
testConvertHtml("<b>bold</b> <br> some text", "bold \\n\\r some text", "simple tag+ new line + some text ");
   
    
   

    });

