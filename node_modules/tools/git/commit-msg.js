"use strict";
var fs = require('fs');

function fail(msg) {
	console.error(msg);
	process.exit(1);
}

var msg = fs.readFileSync(process.argv[2], 'utf8');
if (!/#\d+/.test(msg)) fail("commit rejected: you must include a #nnn issue number in your commit message");
