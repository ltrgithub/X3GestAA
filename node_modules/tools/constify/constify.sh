babel --plugins constify --extensions "._js" --out-dir lib2 lib
for f in `find lib2 -type f`; do mv $f `dirname $f`/`basename $f .js`._js; done
babel --plugins constify --extensions ".js" --out-dir lib2 lib
mv lib lib-old
mv lib2 lib
