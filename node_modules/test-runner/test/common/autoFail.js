"use strict";

import { assert } from 'chai';
Object.keys(assert).forEach(key => { if (key !== 'isNaN') global[key] = assert[key]; });

describe(module.id, () => {

it('Automatic FAIL test', function () {
    ok(false, "FALSE to automatically fail test");
});
});