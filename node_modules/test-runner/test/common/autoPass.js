"use strict";

import { assert } from 'chai';
Object.keys(assert).forEach(key => { if (key !== 'isNaN') global[key] = assert[key]; });

describe(module.id, () => {

it('Automatic PASS test', function () {
    ok(true, "TRUE to automatically pass test");
});
});