"use strict";

module.exports = {
	application: "syracuse",
	contract: "flamegraph",
	entities: {
		flamegraphRecording: require('./entities/recording').entity,
		flamegraphSetting: require('./entities/setting').entity,
		x3Recording: require('./entities/x3Recording').entity,
	},
	representations: {},
};