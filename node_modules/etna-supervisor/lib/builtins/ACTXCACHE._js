"use strict";
var util = require("etna-supervisor/lib/util");

var ACTXPARAM = require("etna-supervisor/lib/builtins/ACTXPARAM");

var cachedClasses = ["ACRY"];

exports.constructor = util.defineClass(function(superv) {
	this.supervisor = superv;
	this.$exported = true;
}, null, {
	init: function(_) {
		cachedClasses.forEach_(_, function(_, name) {
			this[name] = new (ACTXPARAM.constructor)(this.supervisor).init(_, name);
		}, this);
		return this;
	},

})