"use strict";
var glob = require('streamline/lib/globals');

module.exports = {
	OUVRE_TRACE: {
		params: ['COMMENT'],
		body: function(_) {
			var frame = glob.context.x3frame;
			var comment = frame.values.COMMENT;
			//console.log("OUVRE_TRACE stub: " + comment);
		},
	},
	FERME_TRACE: {
		params: [],
		body: function(_) {
			//console.log("FERME_TRACE stub");
		},
	},
	LEC_TRACE: {
		params: [],
		body: function(_) {
			//console.log("LEC_TRACE stub");
		},
	},
};