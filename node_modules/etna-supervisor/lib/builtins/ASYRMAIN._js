"use strict";
exports.$exported = true;

var tracerJs = require('syracuse-core').getTracer("etna.supervisor");

exports.PARVAL = function(_, arcv, paramcod) {
	var paramval = arcv.paramval(_, paramcod);
	tracerJs.debug && tracerJs.debug("PARVAL(" + paramcod + ")=" + paramval);
	return paramval;
};