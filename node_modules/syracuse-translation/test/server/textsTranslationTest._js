"use strict";
var module = QUnit.module;
var fs = require('fs');
var fsp = require("path");
var sys = require("util");
var helpers = require('syracuse-core/lib/helpers');
var uuid = helpers.uuid;

var types = require('syracuse-core/lib/types/allTypes');
var config = require('syracuse-main/lib/nodeconfig').config; // must be first
// syracuse                                                             // require
var dataModel = require("syracuse-orm/lib/dataModel");
var registry = require("syracuse-sdata/lib/sdataRegistry");
var mongodb = require("mongodb");
var streams = require('streamline/lib/streams/streams');
var adminHelper = require("syracuse-collaboration/lib/helpers").AdminHelper;
var jsonExport = require("syracuse-import/lib/jsonExport");
var jsonImport = require("syracuse-import/lib/jsonImport");
var locale = require("syracuse-core/lib/locale");
var textTranslation = require("syracuse-translation/lib/translation");
//
var tracer = console.log;
var rootpath=fsp.dirname(process.cwd());
// var tracer = null;
//
// force basic auth
config.session = config.session || {};
config.session.auth = "basic";
helpers.pageFileStorage = false;

var adminTestFixtures = require("syracuse-collaboration/test/fixtures/adminTestFixtures");
var endPoint = adminTestFixtures.modifyCollaborationEndpoint("mongodb_admin_test");

var baseUrl = "http://localhost:3004";
var contractUrl = "/sdata/syracuse/collaboration/mongodb_admin_test/";
var port = 3004;
var acceptLanguage = "fr,fr-fr";

var profileId;

var cookie = "";


function trace(str) {
    var f = "C:\\trace\\trace_client.log";
  var strn=str+"\n" ;
  var options = {
    flag: 'a+'
  };
    fs.appendFileSync(f, new Buffer(strn, 'utf8'), options);
} 

function trace_update(str) {
    var f = "C:\\trace\\trace_update.log";
  var strn=str+"\n" ;
  var options = {
    flag: 'a+'
  };
    fs.appendFileSync(f, new Buffer(strn, 'utf8'), options);
} 


function onlyInfo(diags) {
    return diags.every(function (diag) {
        return diag.severity == "info";
    });
}

function _getModel() {
    return dataModel.make(
    registry.applications.syracuse.contracts.collaboration,
        "mongodb_admin_test");
}

function getCookie(_, login, pass) {
    var response = new streams.httpRequest({
        url: baseUrl + "/syracuse-main/html/main.html",
        user: login || "guest",
        password: pass || "guest"
    }).end().response(_);
    response.readAll(_);
    strictEqual(response.statusCode, 200, "user authenticated");
    return response.headers["set-cookie"];
}


function post(_, cookie, url, data, statusCode) {
    console.log("post url: "+url);
    var response = streams.httpRequest({
        method: "post",
        url: url.indexOf("http") == 0 ? url : baseUrl + contractUrl + url,
        headers: {
            "content-type": "application/json",
            cookie: cookie
        }
    }).end(JSON.stringify(data)).response(_);

    var responsetext = response.readAll(_);
    //console.log(sys.inspect(response));
    strictEqual(response.statusCode, statusCode || 201, "status verified: ");

    return JSON.parse(responsetext);

}

function put(_, cookie, url, data, statusCode) {
    console.log("PUT  url: " + url);
    var response = streams.httpRequest({
        method: "put",
        url: url.indexOf("http") == 0 ? url : baseUrl + contractUrl + url,
        headers: {
            "content-type": "application/json",
            cookie: cookie
        }
    }).end(JSON.stringify(data)).response(_);
     
    strictEqual(response.statusCode, statusCode || 200, "status verified");
    var resp=response.readAll(_) ;
    //console.log("PUT  resp: " + sys.inspect(resp));
    return JSON.parse(resp);
}

function get(_, cookie, url, statusCode, facet) {
    var type = facet || "generic.$details";
    var response = streams.httpRequest({
        method: "get",
        url: url.indexOf("http") == 0 ? url : baseUrl + contractUrl + url,
        
        headers: {
            cookie: cookie,
            "Accept-Language": acceptLanguage,
            accept: "application/json;vnd.sage=syracuse"
        }
    }).end().response(_);
    strictEqual(response.statusCode, statusCode || 200, "status verified ");
    var resp = response.readAll(_);

   
    return JSON.parse(resp);
}

function del(_, cookie, url, statusCode) {
    var response = streams.httpRequest({
        method: "delete",
        url: baseUrl + contractUrl + url,
        headers: {
            cookie: cookie
        }
    }).end().response(_);
    strictEqual(response.statusCode, statusCode || 200, "status verified");
    return JSON.parse(response.readAll(_));
}

var doStop = false;
module("textTranslation", {
    setup: function () {},
    teardown: function () {
        if (doStop) {
            setTimeout(function () {
                process.kill(process.pid);
            }, 100)
        }
    }
});


asyncTest("init database", 1, function (_) {
    console.log("UT1") ;
    var server = new mongodb.Server(
    endPoint.datasets["mongodb_admin_test"].hostname,
    endPoint.datasets["mongodb_admin_test"].port, {});
    var db = new mongodb.Db(config.collaboration.dataset, server, {});
    db = db.open(_);
    db.dropDatabase(_);
    //
    ok(true, "mongodb initialized");

    start();
});

// start syracuse server
var syracuse;
// wait server initialization
asyncTest("initialize syracuse test server", 1, function (_) {
    syracuse = require('syracuse-main/lib/syracuse');
    syracuse.initializerStatus.on("initialized", function () {
        ok(true, "server initialized");
        syracuse.server.listen(null, port);
        start();
    });
});

var x3sId;
var applicationId;
var adminEp;

asyncTest("create administration endpoint: ", 5, function (_) {
    
    console.log("UT2") ;
    cookie = getCookie(_);
    console.log("create administration endpoint");
    var appli = adminHelper.getApplication(_, "syracuse", "collaboration");
    ok(appli != null, "Application fetch ok");
    applicationId = appli.$uuid;
    console.log("applicationId " + appli.$uuid);
    

  
    var body = post(_, cookie, "endPoints/$template/$workingCopies?trackingId=" + uuid.generate(), {});
    var data = {
        description: "Administration",
        dataset: "mongodb_admin_test",
        enableSearch: false,
        protocol: "syracuse",
        databaseDriver: "mongodb",
        databaseHost: "localhost",
        databasePort: 27017
    };

    data.$key = body.$uuid;
    data.$etag = body.$etag;
    data.applicationRef = {
        $uuid: applicationId
    };

    data.$actions = {
        $save: {
            $isRequested: true
        }
    };
    body = put(_, cookie, body.$url, data);

    adminEp = get(_, cookie, "endPoints('" + body.$uuid + "')");
    // console.log("ADMIN ENDPOINT: "+ JSON.stringify(adminEp,null,2)) ;
    start();
});

 function deleteFolderRecursive(_,path) {
    var files = [];
    if( fs.existsSync(path) ) {
        files = fs.readdir(path,_);
        console.log("files : "+sys.inspect(files));
        files.forEach_(_,function(_,file,idx){
            var curPath = fsp.join(path ,file);
            if(fs.stat(curPath,_).isDirectory()) { // recurse
                deleteFolderRecursive(_,curPath);
            } else { // delete file    
                fs.unlinkSync(curPath);
            }
        });
        console.log("remove empty folder") ;
        fs.rmdir(path,_);
    }
    }


    //asyncTest("test " + profileDescr, test.nbrc, testCheck)} ,10);


asyncTest("index construction", 7, function (_) {
    var context={request: {}} ;
    cookie = getCookie(_);
    context.request.headers={
        host : "localhost",
          "accept" : "application/json",
          "content-type" : "application/json",
          cookie : cookie
        }

    try {
        var testdir = "syracuse-translation/test/server";
        var dpath = fsp.join(__dirname, "resources");

        fs.mkdir(dpath,"7777" ,_) ;
        var inputJson={} ;
        inputJson["key1"] = "First key";
        inputJson["key2"] = "Second key";
        inputJson["key3"] = "Third key";
        var fpath = fsp.join(dpath, "/unitTest-en.json");
        var unitTest="unitTest" ;
        fs.writeFile(fpath, JSON.stringify(inputJson), "utf8", _);

       var appli = adminHelper.getCollaborationApplication(_);
        var ep = adminHelper.getCollaborationEndpoint(_);
        
       
        
        var db = dataModel.getOrm(_, _getModel(), endPoint.datasets.mongodb_admin_test);
        var model = db.model;
       

   
    var body = post(_, cookie, "translationTools/$template/$workingCopies?representation=translationTool.$edit", {
    endpoint: {
         $uuid: ep.$uuid,
         $key: ep.$key,
         description: ep.description
    },
    $actions: {
        fromSyracuseToX3: {
            $isRequested: true,
            $parameters : {
            baseurl: baseUrl,
            dir: testdir,
            indexdir : testdir,
            burl :true,
            unitTestFlg: true 
        }
        }
    }
});




      
        cookie = getCookie(_);
        //console.log("TESTDIR : "+testdir);
        var indexpath=fsp.join( __dirname, "../../../",testdir,"index.json") ;
        //console.log( "indexpath: "+ indexpath) ;
        
        var fd=fs.open(indexpath, "r",null, _);
        var indextmp=fs.readFile(indexpath, "utf8", _);
        var index=JSON.parse(indextmp) ;
        strictEqual(index.count,3, "records counter") ;
        var abspath=fsp.join(__dirname, "resources",unitTest+"-en.json") ;
        var relpath=fsp.relative(rootpath,abspath).replace(/\\/g,'\/') ;

        var obj=index[relpath] ;

        strictEqual(obj["key1"].text,inputJson["key1"] , "key1") ;
        var key2=fsp.join(relpath,"key2").replace(/\\/g,'\/') ;
        strictEqual(obj["key2"].text,inputJson["key2"] , "key2") ;
        var key3=fsp.join(relpath,"key3").replace(/\\/g,'\/') ;
        strictEqual(obj["key3"].text,inputJson["key3"] , "key3") ;
      
        deleteFolderRecursive(_,dpath) ;
        
        fs.close(fd,_) ;
       //fs.unlink(indexpath,_) ;
    } catch (ex) {
        console.error(ex);
    }
    
    console.log() 
    start();
});


function testUpdtSyrFunc(_) {
    tracer=trace ;
    var context={request :{}} ;
    cookie = getCookie(_);
    context.request.headers={
            host : "localhost",
          "accept" : "application/json",
          "content-type" : "application/json",
          cookie : cookie
        }

    try {
        var testdir = "syracuse-translation/test/server";
        var unitTest="unitTest" ;
        var dpath = fsp.join(__dirname, "resources");
        fs.mkdir(dpath,"0777" ,_) ;
        var inputJson={} ;
        inputJson["key4"] = "Forth key";
        inputJson["key5"] = "Sixth key";
        inputJson["key6"] = "Seventh key";
        var fpath = fsp.join(dpath, "/unitTest-en.json");
      
        fs.writeFile(fpath, JSON.stringify(inputJson,null,2), "utf8", _);

       
        var appli = adminHelper.getCollaborationApplication(_);
        var ep = adminHelper.getCollaborationEndpoint(_);
        
        var body = post(_, cookie, "translationTools/$template/$workingCopies?representation=translationTool.$edit",//
         {
            endpoint: {
                $uuid: ep.$uuid,
                $key: ep.$key,
                description: ep.description
            },
            $actions  : {
                fromSyracuseToX3: {
                $isRequested: true,
                $parameters : {baseurl: baseUrl,
                                dir: testdir,
                                indexdir: testdir,
                                burl :true,
                                unitTestFlg: true } 
                                }
            }});
          
           
    

        
       
        
        var db = dataModel.getOrm(_, _getModel(), endPoint.datasets.mongodb_admin_test);
        var model = db.model;
        var techEntity = model.getEntity("translationTool");
        var profileInst = db.fetchInstance(_, techEntity, {
            jsonWhere: {
                code: "test"
            }
        });
        var entity=db.getEntity(_,"APLSTD") ;
        
        tracer && tracer("1st extraction");
      

         var body = post(_, cookie, "translationTools/$template/$workingCopies?representation=translationTool.$edit",//
         {
            endpoint: {
                $uuid: ep.$uuid,
                $key: ep.$key,
                description: ep.description
            },
            $actions  : {
                fromSyracuseToX3: {
                $isRequested: true,
                $parameters : {baseurl: baseUrl,
                                dir: testdir,
                                indexdir: testdir,
                                burl :true,
                                unitTestFlg: true } 
                                }
            }});
        
        var indexpath=fsp.join(__dirname, "../../../",testdir, "index.json") ;
        //console.log( "indexpath: "+ indexpath) ;
        var fd=fs.open(indexpath, "r",null, _);
        var indextmp=fs.readFile(indexpath, "utf8", _);
        var index=JSON.parse(indextmp) ;
        var abspath=fsp.join(__dirname, "resources",unitTest+"-en.json") ;
        var relpath=fsp.relative(rootpath,abspath).replace(/\\/g,'\/') ;
        var obj=index[relpath] ;
        var entity = db.getEntity(_, "APLSTD", "$query");
        var rec=db.fetchInstance(_,entity,{sdataWhere: "LANCHP eq 10031 and LAN eq 'ENG' and LANNUM eq 4"});
        if (!rec)
            tracer && tracer("NON CREE "+relpath+' '+"key4") ;
        else{
            var msg=rec.LANMES(_) ;
            strictEqual(msg,inputJson["key4"] , "before update") ;
        }
        
        var fpath = fsp.join(__dirname, "../server/resources/unitTest-en.json");
        var input = fs.readFile(fpath, "utf8", _);
        var parsed = JSON.parse(input);
        inputJson["key4"] = inputJson["key4"] + " changed";
        fs.writeFile(fpath, JSON.stringify(inputJson,null,2), "utf8", _);

        tracer && tracer("2nd extraction");

       
        var body = post(_, cookie, "translationTools/$template/$workingCopies?representation=translationTool.$edit",//
         {
            endpoint: {
                $uuid: ep.$uuid,
                $key: ep.$key,
                description: ep.description
            },
            $actions  : {
                fromSyracuseToX3: {
                $isRequested: true,
                $parameters : {baseurl: baseUrl,
                                dir: testdir,
                                indexdir: testdir,
                                burl :true,
                                unitTestFlg: true } 
                                }
            }});
       
        rec=db.fetchInstance(_,entity,{sdataWhere: "LANCHP eq 10031 and LAN eq 'ENG' and LANNUM eq "+obj["key4"].id});
        if (!rec)
            tracer && tracer("NON CREE "+relpath+" key4") ; 
        else {
        var msg=rec.LANMES(_) ;
        tracer && tracer("msg="+msg) ;
        strictEqual(msg,inputJson["key4"] , "after update") ;

        var tmp=inputJson["key4"] ;
        inputJson["key4"] = inputJson["key4"] + " conlict";
        fs.writeFile(fpath, JSON.stringify(inputJson), "utf8", _);
        
       
        
        rec.LANMES(_,msg+" conflict1");
        rec.save(_) ;
        
        
        tracer && tracer("3rd extraction");
       

        var body = post(_, cookie, "translationTools/$template/$workingCopies?representation=translationTool.$edit",//
         {
            endpoint: {
                $uuid: ep.$uuid,
                $key: ep.$key,
                description: ep.description
            },
            $actions  : {
                fromSyracuseToX3: {
                $isRequested: true,
                $parameters : {baseurl: baseUrl,
                                dir: testdir,
                                indexdir: testdir,
                                burl :true,
                                unitTestFlg: true } 
                                }
            }});
        
        var diag=body.$diagnoses;
        //profileInst.getAllDiagnoses(_, diag);
        //console.log("*********BODY: "+JSON.stringify(body,null,4)) ;
        //console.log("DIAGNOSES: "+sys.inspect(diag)) ;
        strictEqual(diag[0].severity, "warning" , "conflict warning raised") ;
        strictEqual(diag[0].message, 'update conflict for resource Syracuse/node_modules/syracuse-translation/test/server/resources/unitTest-en.json:key4, choose between  Forth key changed conlict(syracuse version)  and   Forth key changed conflict1(X3 version) ',"warning message") ;
        //restore
        inputJson["key4"] = tmp;
        fs.writeFile(fpath, JSON.stringify(inputJson), "utf8", _);
        
        rec.LANMES(_,tmp);
        rec.save(_) ;
        }

        fs.close(fd,_) ;
        //fs.unlink(indexpath,_) ;

    } catch (ex) {
        console.error(ex);
    }
    start();
}

function testUpdtX3Func(_) {
    tracer= trace_update ;
    var context={request :{}} ;
    cookie = getCookie(_);
    context.request.headers={
            host : "localhost",
          "accept" : "application/json",
          "content-type" : "application/json",
          cookie : cookie
        }

        var testdir = "syracuse-translation/test/server";
        var unitTest="unitTest" ;
        var dpath = fsp.join(__dirname, "resources");
        var fpath = fsp.join(dpath, unitTest+"-en.json");

    try {
        var ep = adminHelper.getCollaborationEndpoint(_);
        var db = dataModel.getOrm(_, _getModel(), endPoint.datasets.mongodb_admin_test);
        var model = db.model;
        tracer && tracer("CollaborationEndpoint: ") ;
        
         var entityApp=db.model.getEntity("application") ;
    
        var appli = adminHelper.getApplication(_, "X3", "erp");
        ok(appli != null, "Application fetch ok");
        //var apX3=db.fetchInstance(_, ent,{jsonWhere: {"dataset": "SUPERV"}}); // instance.endpoint(_).getOrm(_);
   

        var entityServ=db.getEntity(_,"x3server") ;
        var x3server=entityServ.createInstance(_,db) ;
        x3server.serverName(_,"") ;
        x3server.description(_,"sodaix02");
        x3server.serverHost(_,"sodaix02") ;
        x3server.serverPort(_,17000) ;
        x3server.serverTimeout(_,10000) ;
        x3server.save(_);

        var diags=[] ;
        x3server.getAllDiagnoses(_,diags) ;
        tracer && tracer("x3 server diags: "+sys.inspect(diags)) ;
        tracer && tracer("x3 server created") ;

        var entityEp=db.getEntity(_,"endPoint") ;
        var epX3=entityEp.createInstance(_,db);
        epX3.dataset(_, "SUPERV");
        epX3.description(_, "X3ERP");
        epX3.applicationRef(_, appli);
        epX3.x3server(_,x3server) ;
        epX3.x3ServerFolder(_, "superv");
        epX3.x3SolutionName(_, "SOLSUPV6");
        epX3.save(_);

        var diags=[] ;
        epX3.getAllDiagnoses(_,diags) ;
        tracer && tracer("x3 endpoint diags: "+sys.inspect(diags)) ;
        tracer && tracer("x3 endpoint created") ;

        tracer && tracer("**fetch instance *** ") ;
        var epX3=db.fetchInstance(_, entityEp,{jsonWhere: {"dataset": "SUPERV"}}); // instance.endpoint(_).getOrm(_);
        tracer && tracer("**********EP X3 : "+epX3.getBaseUrl(_)) ;


       var dbCollab = adminHelper.getCollaborationOrm(_); 
       var userGuest=dbCollab.fetchInstance(_, dbCollab.model.getEntity("user"), {jsonWhere: {lastName:"Guest"}});
        
        var userEpEntity=db.model.getEntity(_,"userEndpoint") ;
        var userEp=userEpEntity.createInstance(_,db) ;
        userEp.endpoint(_,epX3 ) ;
        userEp.login(_,"dadim") ;
        userEp.save(_) ;

         tracer && tracer("**********userEp login : "+userEp.login(_)) ;

        var userEntity=db.model.getEntity("user") ;
        var user=userEntity.createInstance(_,db );

        user.login(_,userGuest.login(_)) ;
        tracer && tracer("**********user login : ") ;
        user.password(_,userGuest.password(_)) ;
        user.lastName(_,userGuest.password(_)) ;
        user.lastName(_,userGuest.password(_)) ;
        user.endpoints(_).set(_, userEp/*user.createChild(_, "userEndpoints", userEp.$uuid)*/);
        user.save(_);
        tracer && tracer("**********user login : "+user.login(_)) ;




       





        //ok(appli != null, "Application fetch ok");

        var entity=db.getEntity(_,"APLSTD") ;
        var rec=db.fetchInstance(_,entity,{sdataWhere: "LANCHP eq 10031 and LAN eq 'ENG' and LANNUM eq 5"});
        if (rec){
            var expected=rec.LANMES(_)+" updated on X3 side";
            rec.LANMES(_, expected) ;
            rec.save(_)
        }
        rec=db.fetchInstance(_,entity,{sdataWhere: "LANCHP eq 10031 and LAN eq 'ENG' and LANNUM eq 5"});
        
        
        strictEqual(expected , rec.LANMES(_)," updated on X3 side") ;

        var expectedFr="traduction française";
       //create the french translation

       var recFR= entity.createInstance(_,db) ;

        recFR.LANCHP(_, 10031);
        recFR.LANNUM(_, 5);
        recFR.LANMES(_, expectedFr);
        recFR.LAN(_, "FRA");
        recFR.save(_);

        recFR=db.fetchInstance(_,entity,{sdataWhere: "LANCHP eq 10031 and LAN eq 'FRA' and LANNUM eq 5"});
       strictEqual(expectedFr , recFR.LANMES(_),"french translation created on X3 ") ;
        
        tracer && tracer("x3->Syracuse "+baseUrl);
        var body = post(_, cookie, "translationTools/$template/$workingCopies?representation=translationTool.$edit",//
         {
            endpoint: {
                $uuid: ep.$uuid,
                $key: ep.$key,
                description: ep.description
            },
            $actions  : {
                fromX3toSyracuse: {
                $isRequested: true,
                $parameters :  {
            baseurl: baseUrl,
            dir: testdir,
            indexdir: testdir,
            burl :true
        }
            }}});
       

        var input = fs.readFile(fpath, "utf8", _);
        var parsed = JSON.parse(input);
        
        strictEqual(expected , parsed["key5"] ,"Syracuse resource updated: eng") ;
        
        fpath = fsp.join(dpath, unitTest+"-fr.json");
        input = fs.readFile(fpath, "utf8", _);
        parsed = JSON.parse(input);
        
        strictEqual(expectedFr , parsed["key5"] ,"Syracuse resource created: fr") ;
        
        deleteFolderRecursive(_,dpath) ;
         //var indexpath=fsp.join(__dirname, "../../../",testdir, "index.json") ;
        
        //fs.close(fd,_) ;
        //fs.unlink(indexpath,_) ;
        } catch (ex) {
        console.error(ex);
    }
    start();
}

asyncTest("update of the message in syracuse", 9, testUpdtSyrFunc);
asyncTest("update of the message in X3", 11, testUpdtX3Func);






function stopFunc(_) {
    doStop = true;
    start();
}

//setTimeout(function(){
    asyncTest("stop  tests", 0,stopFunc);
//} ,120);