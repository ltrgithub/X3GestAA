"use strict";

exports.entity = {
  plural: "children",
  $properties: {
    firstName: {
      $title: "First Name",
      $type: "string",
      $constraints: {
        $maxLength: 30
      }
    },
    lastName: {
      $title: "Last Name",
      $type: "string",
      $constraints: {
        $maxLength: 30
      },
      $isMandatory: true
    },
    gender: {
      $title: "Gender",
      $type: "integer",
      $enum: [1, 2],
      $enumCaptions: ["Homme", "Femme"]
    },
    birthDate: {
      $title: "Birth Date",
      $type: "date",
      $isMandatory: false
    }
  },
  $titleTemplate: "{firstName} {lastName}",
  $relations: {
    parent: {
      $type: "employee"
    }
  },
  $rules: {},
  $links: {}
};