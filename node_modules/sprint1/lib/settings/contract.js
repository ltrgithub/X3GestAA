exports.contract = {
    application: "sprint1",
    contract: "settings",
    entities: {
        employee: require('./entities/employee').entity,
        country: require('./entities/country').entity,
        company: require('./entities/company').entity,
        child: require('./entities/child').entity,
        test1: require('./entities/test1').entity,
    },
    representations: {
        // representation name here, not entity name (but might be the same as the entity name
        employee: require('./representations/employee').representation,
        employeeMob: require('./representations/employeeMob').representation,
        companyMob: require('./representations/companyMob').representation,
		countryMob: require('./representations/countryMob').representation
    }
}
