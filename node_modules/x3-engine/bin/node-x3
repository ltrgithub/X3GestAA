#!/usr/bin/env node --debug

try {
	require("x3-engine/lib/register");
	var arg = process.argv[2];
	var path = require('path');
	var p = path.join(process.cwd(), arg);
	require(p);
}
catch (ex) {
	console.log("failed: " + ex.stack);
}