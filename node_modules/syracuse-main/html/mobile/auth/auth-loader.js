"use strict"; 
$(function() {
	var $auth = $.auth;
	$auth.resetData = function() {
		$("#json").val('');
	}
	$auth.loadData = function(data) {
		$("#json").val(window.JSON.stringify(data, null,2));
	};
	$auth.sendData = f
	$("#refresh").click(function(e) {
		alert("Do refresh");
		
	});
	$("#save").click(function(e) {
		alert("Do save");
	});
	
	
});