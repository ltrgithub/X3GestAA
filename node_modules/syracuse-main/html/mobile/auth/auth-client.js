"use strict"; 
(function($) {
	$.proto = $.proto || {};
	var $proto = $.proto;
	var $helpers = $.helpers;
	var that = $proto.auth || {};
	$proto.auth = that;
	$proto.auth.notifyParent = function(event, eventData) {
		var $auth = (window && window.parent && (window.parent != window) && window.parent.$ && window.parent.$.auth)?window.parent.$.auth:null;
		if ($auth) {
			return $auth.notify(event, eventData)
		} else {
			return eventData;
		}
	};
})(jQuery); 

	